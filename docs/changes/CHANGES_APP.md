## Change History

### 2026-08-04
#### [FEAT] Add always-on Taste Skill integration for portfolio design
- **Template/File:** `AGENTS.md`, agent adapter files, `docs/PORTFOLIO_TASTE_STANDARD.md`, and `vendor/taste-skill/*`
- **Record:** `e988add20dab0fa97d7a76781c48961c8184288e`
- **What changed:** Vendored the complete tracked Taste Skill snapshot and added repository-local instructions for common coding agents and models.
- **Before:** Taste Skill was not available as a repository-local source or always-on instruction.
- **After:** Public portfolio design tasks use the Portfolio adapter, upstream references, explicit design dials, audit-first redesign guidance, and a required pre-flight checklist.
- **Why:** Keep future portfolio design work consistently intentional, accessible, responsive, and resistant to generic AI UI patterns.
- **Fix/Notes:** Existing Portfolio tokens, routes, dependencies, and functionality remain the source of truth. The upstream snapshot is preserved unchanged under `vendor/taste-skill/`.

### 2026-08-05
#### [FIX] Remove blocking home loader and split the hero
- **Template/File:** `components/HomeClient.jsx`, `components/SiteHeader.jsx`, `components/Intro.jsx`, and `app/globals.css`
- **Record:** `home-hero-loader-split`
- **What changed:** Removed the full-screen greeting preloader from the homepage lifecycle, revealed the header immediately, removed the Delhi map from the hero, and changed the hero to an image-over-paper split composition.
- **Before:** A timer-driven greeting sequence blocked scroll and delayed the first useful view for roughly three seconds. The hero used the Delhi map as its main visual field.
- **After:** The homepage renders directly into the hero. The hero uses the existing local fractal asset in an upper visual half and the current hero message on a solid paper lower half. Existing routes and hero copy remain intact.
- **Why:** The portfolio has no data-loading reason to delay access, and the visual reference calls for a clear split composition with the message grounded in a solid lower stage.
- **Fix/Notes:** The old `Preloader.jsx` file and Delhi map assets remain untouched for reversibility and other possible uses. The home intro context is complete immediately so project content does not wait behind a curtain.

### 2026-08-05
#### [FIX] Use the supplied hero image at viewport height
- **Template/File:** `app/globals.css` and `public/assets/backgrounds/portolio-hero-image*`
- **Record:** `hero-image-viewport-split`
- **What changed:** Replaced the textured abstract fallback and gradient overlays with the supplied `portolio-hero-image` asset. Added a rotated landscape derivative for desktop and retained the original portrait crop for mobile. The hero now uses exactly the available viewport below the fixed header, split into equal visual and copy halves.
- **Before:** The hero used a generated derivative with color overlays and could grow taller than the first viewport because the copy determined its height.
- **After:** The supplied image is used without vertical blur lines or overlay treatment. Desktop uses the rotated image to fill its half; mobile uses the original portrait image. The split is constrained to `calc(100dvh - var(--site-header-h))`.
- **Why:** Match the supplied reference more faithfully and make the first view feel immediate and composed.
- **Fix/Notes:** The existing `hero-abstract.webp` remains in the repository but is no longer referenced by the hero.

### 2026-08-05
#### [FEAT] Left-align hero typography and add diagonal technology marquee
- **Template/File:** `components/Intro.jsx`, `app/globals.css`, `app/layout.tsx`, and `public/fonts/Fluctuation-Lt.otf`
- **Record:** `hero-typography-logo-marquee`
- **What changed:** Replaced the rotating hero icon slots with one consistent Fluctuation type treatment, moved the hero copy and CTA to a left-aligned stack, and added a diagonal two-rail technology logo marquee with faded edges in the paper half.
- **Before:** Hero words used multiple decorative font treatments and rotating technology icons inline with the headline. The paper half had no separate technology proof surface.
- **After:** The hero message uses one display family, reads as clear left-aligned lines, and the technology marks move independently below the copy. The marquee uses transform-only CSS motion and becomes static for reduced-motion users.
- **Why:** Improve readability and hierarchy while retaining a visual expression of the engineering toolset without making the headline wait on or compete with rotating logos.
- **Fix/Notes:** The supplied `fluctuation.zip` contained the Fluctuation font files and its EULA; the font is self-hosted through `next/font/local`. Logo marks remain local package icons, with no new runtime dependency or external image request.

### 2026-08-05
#### [FIX] Move technology marquee beside hero copy
- **Template/File:** `app/globals.css` and `components/Intro.jsx`
- **Record:** `hero-side-marquee`
- **What changed:** Reduced the desktop hero text scale and left inset, changed the paper half to a two-column layout, and moved the diagonal logo marquee into the right column. Mobile keeps the marquee below the text as the explicit fallback.
- **Before:** The lower paper half stacked the large text and marquee vertically.
- **After:** Desktop presents a quieter left copy column and a separate right-side diagonal technology field, while the hero retains its fixed viewport height and edge fades.
- **Why:** Improve scanability and give the technology proof its own visual zone without competing with the headline.
- **Fix/Notes:** No route, copy, font, or reduced-motion contracts changed.

### 2026-08-05
#### [FIX] Make the side marquee icon-only and same-direction
- **Template/File:** `components/Intro.jsx` and `app/globals.css`
- **Record:** `hero-icon-marquee`
- **What changed:** Removed logo labels, tilted every icon tile consistently, and made both diagonal rows travel in the same direction with different durations.
- **Before:** The marquee displayed icon and text pairs, with the second row moving in reverse.
- **After:** The right-side marquee is an icon-only field with consistent negative tilt, faded horizontal edges, and two same-direction speeds. Reduced-motion users see a static tilted arrangement.
- **Why:** Match the provided visual reference more closely and keep the technology field graphic rather than typographic.
- **Fix/Notes:** The implementation remains transform-only CSS motion and preserves the mobile stacked fallback.

### 2026-08-05
#### [FIX] Speed up and normalize icon marquee motion
- **Template/File:** `components/Intro.jsx` and `app/globals.css`
- **Record:** `hero-icon-marquee-fast-ltr`
- **What changed:** Set both diagonal icon rows to the same fast `18s` duration, changed both tracks to travel left-to-right, removed the green marquee tint, and increased the icon sizes.
- **Before:** The rows used different durations and moved leftward, with a subtle green background wash.
- **After:** Both rows move left-to-right at the same speed over the neutral paper surface, with larger consistently tilted icons and the existing edge fade.
- **Why:** Match the requested motion direction and make the technology field more legible and graphic.
- **Fix/Notes:** Reduced-motion behavior remains static and no page layout or route contracts changed.

### 2026-08-05
#### [FEAT] Expand technology marquee coverage
- **Template/File:** `components/Intro.jsx`
- **Record:** `hero-icon-marquee-technology-coverage`
- **What changed:** Added Microsoft Foundry, Copilot, Copilot Studio, Azure AI, Azure Bot Service, Power Automate, Azure Functions, SQL Server, VS Code, LangChain, and n8n to the repeated icon sequence.
- **Before:** The marquee covered 12 core tools and platforms.
- **After:** The marquee contains 23 technology marks, duplicated for a seamless infinite loop with the existing same-speed left-to-right motion.
- **Why:** Better represent the portfolio's AI, Azure, automation, developer tooling, and data stack.
- **Fix/Notes:** Reused existing `react-icons/si` dependencies; no new package or external asset request was added.

### 2026-08-05
#### [FIX] Split marquee into colorful unique technology rows
- **Template/File:** `components/Intro.jsx` and `app/globals.css`
- **Record:** `hero-icon-marquee-unique-colorful-rows`
- **What changed:** Split the expanded technology catalog into two non-overlapping row sets, added additional AI, cloud, data, frontend, automation, and observability marks, applied each brand color, and slowed both infinite tracks to the same `32s` speed.
- **Before:** Both rows used the same full logo catalog, so technologies repeated across rows and icons were muted monochrome.
- **After:** Each row has a unique visible technology set, with 45 total technology marks across the two rows. Each row duplicates its own sequence internally only to make the left-to-right loop seamless.
- **Why:** Improve technology coverage and visual recognition while keeping the marquee infinite and calm enough to read as a considered tool field.
- **Fix/Notes:** Existing tilt, edge fade, responsive collapse, and reduced-motion behavior remain unchanged.

### 2026-08-05
#### [FIX] Straighten and enlarge marquee icons
- **Template/File:** `components/Intro.jsx` and `app/globals.css`
- **Record:** `hero-icon-marquee-straight-large`
- **What changed:** Removed the diagonal rail rotation and per-icon tilt, then increased desktop and mobile icon sizing.
- **Before:** Both rows and each icon were rotated for a diagonal treatment.
- **After:** Both rows remain horizontal and all marks sit upright, with larger colorful icons and the same infinite left-to-right motion.
- **Why:** Improve logo recognition and follow the requested clean horizontal treatment.
- **Fix/Notes:** Row separation, edge fading, same-speed animation, reduced-motion fallback, and mobile overflow protection remain in place.

### 2026-08-05
#### [FIX] Give cloud headline and marquee rows explicit separation
- **Template/File:** `components/Intro.jsx` and `app/globals.css`
- **Record:** `hero-cloud-line-unique-marquee-rows`
- **What changed:** Wrapped the headline into explicit lines so `Cloud infrastructure.` always starts on its own line, and made the two marquee row partitions explicit through separate slices of the technology catalog.
- **Before:** Cloud could wrap as an independent flex item and the row split was implicit in the shared sequence.
- **After:** The headline has deliberate line grouping, and each marquee row receives a unique technology set while duplicating only within its own row for the infinite loop.
- **Why:** Improve headline rhythm and prevent accidental cross-row logo repetition.
- **Fix/Notes:** The title remains responsive, both marquee rows stay colorful and infinite, and the mobile fallback remains unchanged.

### 2026-08-05
#### [FIX] Make marquee looping seamless
- **Template/File:** `components/Intro.jsx` and `app/globals.css`
- **Record:** `hero-icon-marquee-seamless-loop`
- **What changed:** Replaced percentage-offset looping with two explicit identical sequence elements per row. Each track now translates by exactly one sequence width from `-50%` to `0%`.
- **Before:** The duplicated items lived in one flex track with a shared gap, so `50%` did not exactly equal the distance between the two sequence starts and the loop could visibly reset.
- **After:** The second sequence is structurally identical to the first and the reset happens at an identical visual frame, producing a continuous infinite marquee in both rows.
- **Why:** Remove the visible loop break and make the motion genuinely seamless.
- **Fix/Notes:** Row uniqueness, colors, speed, direction, tilt removal, edge fades, and reduced-motion behavior remain intact.

### 2026-08-05
#### [FIX] Keep Microsoft AI marquee unique and evenly spaced
- **Template/File:** `components/Intro.jsx` and `app/globals.css`
- **Record:** `hero-ai-marquee-spacing-and-stack`
- **What changed:** Removed non-AI infrastructure and generic data logos such as Kubernetes, SQL Server, TensorFlow, PyTorch, databases, and unrelated frontend tools. Split the marquee into an AI/agent row and a Microsoft stack row, including Foundry, Azure, Copilot, Copilot Studio, Azure AI, Azure Bot Service, Azure DevOps, Power Platform, Microsoft 365, and related services. Added exact per-sequence trailing spacing to the duplicated tracks.
- **Before:** Azure-family marks appeared multiple times, the rows mixed unrelated infrastructure logos, and the loop boundary could change spacing when it restarted.
- **After:** Each row has a focused technology category with no cross-row name overlap. The duplicate sequence includes the same trailing gap as the first, so the `-50%` reset lands on an identical spacing frame.
- **Why:** Keep the marquee relevant to AI agents and the Microsoft ecosystem while eliminating the visible restart jump.
- **Fix/Notes:** Both rows retain colorful marks, same-speed infinite motion, upright icons, faded edges, and reduced-motion behavior.

### 2026-08-05
#### [FIX] Remove repeated Azure marks and focus marquee on AI stacks
- **Template/File:** `components/Intro.jsx` and `app/globals.css`
- **Record:** `hero-ai-microsoft-marquee-cleanup`
- **What changed:** Rebuilt the marquee data into two explicit categories: AI/agent tooling and Microsoft ecosystem tooling. Removed Kubernetes, SQL Server, TensorFlow, PyTorch, database, frontend, and unrelated infrastructure marks. Preserved only one named Azure entry and used dedicated local Azure AI, Copilot Studio, and Azure Bot Service assets for those distinct products.
- **Before:** Azure appeared under several repeated generic icon entries, rows mixed unrelated technologies, and hidden duplicate labels made runtime auditing ambiguous.
- **After:** The two rows have no cross-row name overlap, the AI row contains agent/model tooling, and the Microsoft row contains Azure, Foundry, Copilot, Copilot Studio, Azure services, Power Platform, Dynamics, Microsoft 365, and related marks. Each sequence includes its own exact trailing gap for a stable restart.
- **Why:** Make the marquee accurate to the requested AI-agent and Microsoft stack focus and remove repeated Azure confusion.
- **Fix/Notes:** Both rows remain colorful, upright, infinite, same-speed, and responsive. Hidden duplicate sequence labels are no longer rendered for assistive technology.

### 2026-08-05
#### [FEAT] Add marquee technology tooltips and hover slowdown
- **Template/File:** `components/Intro.jsx` and `app/globals.css`
- **Record:** `hero-marquee-tooltips-hover-speed`
- **What changed:** Added a visible technology-name tooltip on icon hover and keyboard focus. Hovering or focusing the marquee slows both tracks from `32s` to `72s` without changing the loop structure.
- **Before:** Icons had no visual name affordance and the marquee kept its default speed during inspection.
- **After:** Each visible icon exposes its name in a compact tag, while hover/focus slows the moving field so the mark can be identified. Touch devices keep the labels hidden to avoid tap-triggered tooltip clutter.
- **Why:** Make the dense technology field understandable and easier to inspect without interrupting the infinite motion.
- **Fix/Notes:** Reduced-motion users still receive a static marquee. Existing row uniqueness, colors, spacing, and overflow protection remain intact.

### 2026-08-17
#### [FEAT] Shader-gradient hero choreography and work-ledger rework
- **Template/File:** `components/Intro.jsx`, `components/hero/HeroGradient.jsx`, `components/Projects.jsx`, `components/work/WorkIndex.jsx`, `app/globals.css`, `.gitignore`, `package.json`
- **Record:** `hero-shadergradient-intro-and-work-ledger`
- **What changed:** Replaced the previous hero (static photo, headline block, and two-row technology marquee) with a ShaderGradient WebGL field and a scripted load sequence: the field holds full-bleed for 1s, "Hello" wipes in from the right at viewport centre, the field then slices down to a slim vertical rule that lands under the cursor (viewport centre if the pointer has not moved), and the name plus role lines rise in the left column. On scroll, the rule recolours to the work orange and scales out from that exact resting position until it fills the viewport, handing off to the Projects section which shares the colour. Rebuilt the work section as a five-project ledger matching the supplied reference: spine at 60.5%, right-aligned titles, numbered chips straddling the spine, category and description copy to the right, and a left meta bracket plus arrow on the active row.
- **Before:** Hero was a photo-backed grid with a headline and an infinite icon marquee; the work section was a black `ProjectListMenu` list of 19 projects with a simple fade-in.
- **After:** Hero is a single choreographed gradient-to-rule sequence; the work section is an orange editorial ledger with hover-driven state.
- **Why:** Requested animation rework — replace the intro, drive a gradient-to-rule-to-fill narrative, and match the reference work layout.
- **Fix/Notes:** Row-separator behaviour is implemented as N+1 independent rules with a lit set of `{active, active+1}`, so the rule shared by two adjacent rows is never re-animated when moving between them; only the outgoing top rule and the incoming bottom rule change. Verified lit sets move `{0,108} -> {108,216} -> {216,324}`. The filled chip and the spine marker slide vertically by exactly one row height via `gsap.quickTo`. Accent is `--work-orange: #cc3a0d`, chosen for 5.03:1 contrast against white so body copy clears WCAG AA. Reduced-motion collapses to the resting state with no shader canvas, no greeting, and no scroll wipe. Mobile (<768px) drops the spine, chip and meta to a single column, keeps descriptions visible, and shows no horizontal overflow. The previous `components/ProjectListMenu.jsx` and `.module.css` are now unreferenced but were left in place rather than deleted. Pre-change copies of the edited files are in the git-ignored `.backup-animations/`.

#### [CONFIG] Add ShaderGradient and pin React 19-compatible three stack
- **Template/File:** `package.json`
- **Record:** `shadergradient-dependency-add`
- **What changed:** Added `@shadergradient/react@2.4.20`, `three@^0.169.0`, and `@react-three/fiber@^9.0.0` (installed with `--legacy-peer-deps`).
- **Before:** No WebGL gradient dependency; `ogl` was the only 3D-adjacent package.
- **After:** The hero shader is dynamically imported so the three/fiber stack stays out of the initial bundle.
- **Why:** Required for the requested ShaderGradient hero background.
- **Fix/Notes:** `@shadergradient/react` externalises `three` and `@react-three/fiber` rather than bundling them, so both must be installed explicitly. `@react-three/fiber@8` was tried first to match the declared `react@18.2.0` but crashed at runtime with `Cannot read properties of undefined (reading 'ReactCurrentOwner')`: Next 16 serves its own vendored React 19.3 canary to the client, so the runtime React is 19 regardless of the declared dependency. fiber 9 is therefore the correct pairing; `--legacy-peer-deps` is needed only because `package.json` still declares React 18. The package's `lazyLoad` default of `true` was disabled — its IntersectionObserver dropped the canvas once the field was clipped to the rule — and the canvas is now unmounted deliberately via an `active` prop when the hero scrolls out.

### 2026-08-17
#### [FEAT] Header pill, cursor-tracked hero rule, and band-clipped work section
- **Template/File:** `components/SiteHeader.jsx`, `components/Intro.jsx`, `components/work/WorkIndex.jsx`, `app/layout.tsx`, `app/page.jsx`, `app/globals.css`
- **Record:** `hero-rule-follow-and-work-band`
- **What changed:** Six related adjustments. (1) The header is suppressed for the duration of the hero load sequence. (2) The header is now a floating capsule in the top-right containing only Blog and Resume; the wordmark and the local clock were removed. (3) While the hero is at rest the rule trails the cursor, stretching in proportion to the distance still to cover and relaxing back to its base width on arrival. (4) The work section is clipped to the expanding orange band, so its content is only visible inside the band as it opens. (5) The number boxes no longer travel; each fills and empties in place. (6) The row separators grow and retract from the spine intersection rather than the page edge.
- **Before:** Full-width black header bar with wordmark and clock, visible throughout the intro; the rule was static once it came to rest; the work section appeared as a whole rectangle behind the wipe; a single white chip slid vertically between number boxes; separators scaled from `transform-origin: left`.
- **After:** The pill appears only once the hero finishes; the rule is a live cursor-tracked element; the work section is revealed through the band; boxes fill in place; separators open outward from the spine.
- **Why:** Requested follow-up refinements to the hero and work animations.
- **Fix/Notes:** Header suppression uses an `intro-running` class set by the existing inline script in the root layout before first paint, so the pill never flashes, and cleared by the hero when its copy reveals. The script carries a 9s failsafe removal so the header cannot be lost if the hero never mounts. `page-top-offset` was removed from the home page because the pill no longer occupies layout space, letting the hero run full-bleed. The rule was converted from a clip-path window on the live shader to a DOM element carrying the same gradient: the shader still performs the one-off slice, then hands off, so cursor tracking and stretching are pure `transform` writes. The scroll sequence is driven from a single `ScrollTrigger.onUpdate` progress value rather than a scrubbed tween, so the rule and the work band are computed from one source and cannot drift; Lenis already smooths the scroll. Band geometry is published as `--band-x` / `--band-h` on the document element and consumed by `.work-section` via `clip-path`, defaulting to `100%` (fully open) so the section is never trapped behind a closed band under reduced motion or if the hero never runs. Verified: rule stretches to ~2.7x mid-flight and returns to 1.0 on arrival; box positions are byte-identical across hover changes; separator `transform-origin` resolves to 801.5px of a 1325px list (60.5%, the spine); lit separator set moves `{0,108} -> {108,216}` with the shared rule untouched. The work counter is hidden below 768px, where it would sit under the fixed pill and reports a hover state touch does not have. Removing the wordmark also removes the header link back to `/`; the footer remains the home path from sub-pages.

### 2026-08-17
#### [FIX] Halo preset, fill timing, greeting persistence, and stronger rule stretch
- **Template/File:** `components/Intro.jsx`, `components/hero/HeroGradient.jsx`, `app/globals.css`
- **Record:** `hero-halo-and-fill-timing`
- **What changed:** (1) The scroll fill now completes exactly as the work section reaches the top of the viewport instead of finishing early. (2) The greeting no longer reappears after the hero settles. (3) The cursor-tracked rule expands far more while travelling. (4) Swapped the bespoke shader configuration for the upstream "Halo" preset, whose gradient the resting rule now mirrors before turning solid orange on scroll.
- **Before:** The fill reached full coverage around 80% of the hero scroll and then sat idle; "Hello" reappeared once the hero finished; the rule stretched to at most ~2.6x in practice; the shader used a three-hue indigo/orange/blue configuration.
- **After:** Coverage lands with the work section at the top; the greeting stays retired; the rule peaks near 9x and eases back; the hero is a simple warm Halo gradient that runs into the work orange.
- **Why:** Requested refinements to pacing, correctness, and art direction.
- **Fix/Notes:** The greeting bug was a genuine ordering fault, not a styling issue: the scroll handler wrote `autoAlpha` to `[copy, hello]` on every update, so at progress 0 it computed `1 - 0` and resurrected the element the intro timeline had just retired. `hello` was removed from that write. The fill previously multiplied its reach by 1.06 as a seam guard, which meant full coverage was crossed before the end of the scroll; that is now a flat 2px epsilon, so coverage and the end of the trigger coincide, and the expansion phase was extended from `0.05-0.8` to `0.05-1.0`. Raising the stretch cap alone did nothing measurable, because the `quickTo` easing on `scaleX` lagged behind a distance that collapses as the rule catches up, so the tween never approached the cap; the stretch is now integrated per frame with an asymmetric coefficient (0.34 attack, 0.07 release) against a slower 0.7s follow, giving fast expansion and a gentle settle. Verified in production: greeting `visibility: hidden` after the hero; stretch peaks at 8.87 and returns to 1.0; the work clip opens at `projTop = -35` (within one 95px scroll step of the top); zero page errors. Halo is applied with `grain: "off"` because at this scale the preset's grain reads as noise. The `.hero-rule` CSS gradient stops were restaged to Halo's palette so the shader-to-DOM hand-off stays invisible; the shader itself now unmounts at ~4s once the rule takes over, with the live canvas confirmed present across the full-bleed and slice phases.

### 2026-08-17
#### [FEAT] Pinned work outro, centred hero copy, thicker rule
- **Template/File:** `components/work/WorkStage.jsx`, `lib/work-stage-context.js`, `components/work/WorkIndex.jsx`, `components/Intro.jsx`, `app/page.jsx`, `app/globals.css`
- **Record:** `work-outro-stage`
- **What changed:** (1) The hero name block moved to the vertical centre of the viewport, unchanged horizontally. (2) The cursor-tracked rule is twice as thick (6px to 12px). (3) Scrolling past the work section no longer moves the page straight away. The section pins, and the scroll first strips the projects out one at a time: each row's title, meta, description and arrow fade upward while its number box fills and latches as a spent marker, and stripped rows can no longer be brought back by hovering. Once the list is reduced to numbers, rules and the spine, the following section wipes down over the top of it, so the next screen arrives from above instead of scrolling up from below.
- **Before:** The hero copy sat at the bottom of the viewport, the rule was 6px, and the work section scrolled away normally into Achievements.
- **After:** Centred hero copy, a 12px rule, and a pinned outro that dismantles the ledger before revealing Achievements from the top edge.
- **Why:** Requested changes to hero composition and the work-to-next-section transition.
- **Fix/Notes:** New `WorkStage` component wraps the work section and whatever follows it in a pinned 100dvh stage with two absolutely-positioned layers. `app/page.jsx` now renders `<WorkStage next={<Achievements />}><Projects /></WorkStage>`. Per-frame work is done with direct DOM writes against a small attribute contract (`[data-work-row]`, `[data-erase]`) so a scroll frame never triggers a React render; only the whole-number erased count is lifted into state, via `lib/work-stage-context.js`, and it changes at most five times across the sequence. Two real bugs were found and fixed during verification. First, the erased count was computed as `Math.floor(clamp(0, 1, p / per))`, which clamped the ratio rather than the count and pegged it at 1, freezing the counter at "02 / 05"; it now clamps the count. Second, the reveal layer was set to `z-index: 2` but stayed invisible, because the layer wrappers have `z-index: auto` and create no stacking context, so the work section's own `z-10` competed directly with it and won; the reveal layer is now `z-index: 30`. The hover-state opacity transitions are disabled while dismantling, since they low-pass the per-frame inline writes and make erased content lag the scroll. The reveal completes at 92% of the pin rather than 100% so the incoming section is held fully visible for a beat instead of unpinning the instant it lands. Below 768px and under reduced motion no pin is created at all: `gsap.matchMedia` leaves both sections in ordinary document flow, verified as `data-staged="false"`, `clip-path: none`, no rows erased, and no horizontal overflow. Verified in production: hero block centre within 3px of the viewport midpoint, rule 12px, `#projects` held at top 0 across the whole sequence, strip `00000 -> 11000 -> 11111`, counter `01 -> 03 -> 05`, reveal `100% -> 0%`, zero page errors, and every section id still reachable to the foot of the page.

### 2026-08-17
#### [FIX] Hero first-paint order, rule jitter on scroll, and settle on return
- **Template/File:** `components/Intro.jsx`, `app/layout.tsx`, `app/globals.css`
- **Record:** `hero-paint-order-and-rule-smoothing`
- **What changed:** (1) On first load the hero copy no longer appears before the sequence runs; the gradient shows first, then the greeting, then the copy. (2) The rule no longer jitters on the way into the work section. (3) Scrolling back up returns the rule at its base width and lets it settle on the pointer before the stretch response is re-armed.
- **Before:** The finished hero painted for a beat on load; the rule fought itself on the transition into the fill; returning to the top snapped the rule straight into a full-strength stretch toward the cursor.
- **After:** Gradient-only until 1s, greeting 1.5-2.9s, copy and header from ~3.8s; the rule tracks the fill continuously; the return glides back at base width and only then re-arms.
- **Why:** Reported load-order, smoothness, and scroll-back issues.
- **Fix/Notes:** The load-order fault was a first-paint flash, not a timeline problem: GSAP hides the copy in a layout effect, which runs only after hydration, so the server HTML painted the finished hero first. A `hero-unpainted` class is now set alongside `intro-running` by the existing inline script before first paint and cleared by the hero the moment its initial states are set. The two flags need separate lifetimes — `intro-running` must persist for the whole sequence to keep the header pill away, whereas the copy guard has to be released immediately or it would also suppress the greeting it exists to sequence. A first attempt gated both on `intro-running` and targeted `.hero-hello` (the inner span) rather than the wrapper GSAP actually animates, which suppressed the greeting entirely for its whole window; the guard now targets `.hero-hello-layer` and the copy lines. The jitter had a concrete cause: `gsap.quickTo` kept a live tween on the rule's `x` while the scroll handler wrote `x` with `gsap.set` on the same frames, so two writers fought every frame. Both were replaced by a single always-on ticker that owns `x` and `scaleX` and smooths them manually, with mode-dependent coefficients (follow: 0.12 position, 0.34 attack / 0.07 release; fill: 0.3 position, 0.24 scale). Because there is now one writer and one continuous state, no mode change can produce a snap. That renderer also reads progress from the ScrollTrigger directly instead of having it pushed in from `onUpdate`: `onUpdate` only fires for scrolls ScrollTrigger hears about, and a programmatic jump that bypasses Lenis left the pushed value stale and stranded the rule mid-fill at scale 173 — reproduced, then fixed. The trigger is held in a pre-declared `let` so the renderer cannot hit a temporal dead zone. Return-to-top sets a `settling` flag that pins the target scale at 1 until the rule is within 2px of the pointer. Verified in production: load order 0/0/0 at 150ms and 700ms, greeting 0.96 at 1.5s and 1.00 at 2.3s, copy and header 1.00 by 4.7s; 122 sampled frames across the fill with 1 scale reversal and 0 position jumps; return peak scale 1.00 with the rule settling at the pointer; stretch re-arms to 8.96 afterwards; reduced motion and mobile both show the copy immediately with no guard class left set and no horizontal overflow; zero page errors.

### 2026-08-19
#### [REFACTOR] Replace the skills gas chamber with a multi-ring solar system
- **Template/File:** `components/Skills.tsx`, `components/skills/OrbitImages.jsx`, `components/skills/OrbitImages.css`, `components/skills/SkillOrbits.jsx`, `data/skillsData.jsx`, `app/globals.css`, `eslint.config.mjs`
- **Record:** `skills-orbit-solar-system`
- **What changed:** The skills section no longer uses the d3-force "gas chamber" simulation. It is now a solar system: four orbital rings, one per skill group, circling a shared centre on a common tilted plane. Rings run inward to outward as Tooling (4), Cloud and data (9), Web and product (10), AI and automation (13), with different radii, periods and directions so the field never settles into a repeating pattern. Faint orbit tracks are drawn so the arrangement reads as a system rather than a scatter.
- **Before:** A 1155-line component running a d3-force simulation with brain-zone clustering, connection edges, drag handling and a separate mobile cluster-wheel layout.
- **After:** A ~100-line section rendering `SkillOrbits` plus a grouped skill list, with the inventory extracted to `data/skillsData.jsx`.
- **Why:** Requested change from the gaseous behaviour to a solar-system layout, based on the React Bits OrbitImages component.
- **Fix/Notes:** OrbitImages was vendored rather than installed, with two deliberate local changes. Its imports were pointed at `framer-motion` instead of `motion/react`: the `motion` package is not installed, framer-motion v11 already ships `motion`, `useMotionValue`, `useTransform` and `animate`, and adding the separate package would have duplicated the animation runtime for no gain. An `items` prop accepting React nodes was added alongside `images`, because the skill glyphs are react-icons components rather than image URLs; the internal OrbitItem already rendered a node, so this only exposes what was there. The upstream `responsive` mode was deliberately not used: it forces a 1:1 container, which around a deliberately flat orbital plane would have produced a very tall, mostly empty section. `SkillOrbits` instead measures once and scales a single shared stage, so all four rings stay concentric and the section keeps a wide, short footprint. Ring radii (190/350/510/660) and the 0.46 vertical flatten were chosen so adjacent rings clear each other's `itemSize` at the vertical extremes. Accessibility: the orbit is `aria-hidden` inside OrbitImages and its glyphs are non-focusable spans, so the same inventory is rendered as a real grouped list underneath — that list is the sole presentation below 768px, where a wide orbital plane cannot be read, and it is available to assistive technology at every width. Reduced motion pauses every ring via the component's own `paused` prop, verified stationary. The previous implementation is preserved at `.backup-animations/Skills.tsx`, and that directory was added to the eslint ignore list so the backup no longer reports warnings. Verified in production: 4 rings, 4 orbit paths, 36 orbiting items, 36 list items, items confirmed in motion, zero page errors, and no horizontal overflow at 390px.

#### [FIX] Skew the skill orbit rings to match the reference proportion
- **Template/File:** `components/skills/SkillOrbits.jsx`, `app/globals.css`
- **Record:** `skills-orbit-skew`
- **What changed:** Flattened the orbital plane so the rings read as skewed ellipses rather than concentric circles. Vertical squash went from 0.46 to 0.26, tilt from -14 to -10 degrees, radii from 190/350/510/660 to 200/370/530/670, and item sizes from 46-56 to 44-50. The design stage height dropped from 760 to 540, and the central star from 190px to 160px.
- **Before:** Rings were too round; at a 0.46 ratio the arrangement read as a stack of concentric circles seen face-on.
- **After:** A flat, tilted orbital plane matching the supplied reference.
- **Why:** Requested skewed rings; the reference composition uses radiusY / radiusX of 80 / 340, roughly 0.235.
- **Fix/Notes:** 0.26 rather than the reference's exact 0.235 because the reference carries a single ring, where item size can exceed the ring's vertical radius without consequence. Across four rings that ratio collapses the vertical spacing, so 0.26 is the point where the rings stay individually legible while the plane still reads as skewed. Radial spacing is set for the left and right extremes, which is where a flat ellipse is actually read; near the top and bottom the rings sit closer and glyphs pass one another, which is correct for an orbital plane at this angle rather than something to design out. Outer radius plus half its item size stays inside BASE_WIDTH / 2, and the rotated vertical extent of the outer ring (about 207px at this tilt) sits inside the 540 stage, so nothing is clipped — confirmed by measuring every orbiting item against the viewport. The flatter plane also shortened the section from 782px to 555px, which brings the grouped skill list into the same viewport as the orbit. Verified in production: 4 rings, 36 orbiting items, 36 list items, no clipping left or right, items in motion, reduced motion stationary, mobile still list-only with no horizontal overflow, zero page errors.

#### [FIX] Remove the visible skill chip grid from the desktop skills section
- **Template/File:** `components/Skills.tsx`
- **Record:** `skills-hide-chip-grid`
- **What changed:** The grouped skill chip grid below the orbit is no longer rendered visually at 768px and above. It remains in the document as screen-reader-only content, and stays fully visible below 768px.
- **Before:** The chip grid was visible beneath the orbit at every width.
- **After:** Desktop shows only the heading and the orbital plane; the section is shorter as a result.
- **Why:** Requested removal of that block.
- **Fix/Notes:** Applied as `md:sr-only` rather than deleting the markup, because that block was doing two jobs beyond its visual one. It is the only skills content below 768px, where the orbit is hidden and a wide orbital plane cannot be read — deleting it outright would have left an empty section with nothing but a heading on mobile. It is also the accessible reading of the orbit, which is `aria-hidden` inside OrbitImages and whose glyphs are non-focusable spans, so removing it would have taken the entire skill inventory off the page for assistive technology and for crawlers indexing the portfolio's technology keywords. Verified in production: at 1440px the container measures 1x1 with `position: absolute`, `overflow: hidden` and `clip: rect(0,0,0,0)` while all 36 list items remain in the DOM, the section is not inside any `aria-hidden` subtree, and the skill text is still reachable via innerText; at 390px the container returns to static flow at full width with no horizontal overflow. Zero page errors.

#### [FIX] Eliminate the SVG hydration mismatch behind the terminal error wall
- **Template/File:** `components/Achievements.jsx`, `components/SiteFooter.jsx`
- **Record:** `svg-hydration-float-precision`
- **What changed:** Computed SVG geometry is now snapped to 3 decimal places before it reaches an attribute or a path string, via a small `snap()` helper in each file.
- **Before:** Raw `Math.sin`/`Math.cos` doubles were rendered directly into SVG attributes and template-literal path data. Node and the browser can serialise the same double to different strings (e.g. `-105.65509926170148` against `-105.6550992617015`), which React reports as a hydration mismatch and prints as a very large element diff on every page load.
- **After:** Both sides emit identical markup. Console is clean in dev and production.
- **Why:** Reported large error in the terminal on opening the project.
- **Fix/Notes:** Two separate things were in that output and only one was ours. The enormous diff also carried `data-darkreader-inline-stroke` and `--darkreader-inline-stroke` attributes; `darkreader` appears nowhere in this repository, so those are injected by the Dark Reader browser extension mutating SVG fill and stroke before React hydrates. React's own message lists that case explicitly and it is not fixable from application code — but it was amplifying a real mismatch underneath, which is what this change fixes. The trailing `THREE.WebGLProgram: Program Info Log: WARNING: Output of vertex shader ... not read by fragment shader` lines come from the vendored ShaderGradient material declaring varyings its fragment shader does not consume. They are warnings, not errors, originate inside `@shadergradient/react` rather than this codebase, and have no runtime effect. Covered call sites: 8 JSX attributes in Achievements and, in SiteFooter, 6 JSX attribute groups plus 8 variable declarations that feed template-literal path data (wave, radial, ladder, blob and spoke glyphs). Verified: 0 console errors and 0 hydration errors across the hero, work, achievements, skills and contact sections in both dev and a production build, with the footer micrographics rendering unchanged.

#### [REFACTOR] Skills as eight planets with hover-revealed moons
- **Template/File:** `data/skillsData.jsx`, `components/skills/SkillOrbits.jsx`, `components/skills/OrbitImages.jsx`, `components/Skills.tsx`, `app/globals.css`
- **Record:** `skills-planets-and-moons`
- **What changed:** The orbit now carries eight large planets, two per ring, instead of all thirty-six skills. Hovering a planet enlarges it and throws out its moons, which travel outward along their own axis and fade in one after another. Every ring stops while a planet is open. The data model was restructured from four flat groups into eight planets each owning its moons.
- **Before:** All 36 skills orbited independently across four rings, every one of them animated continuously.
- **After:** Eight animated items at idle; the 28 moons are static DOM that only transitions on hover.
- **Why:** Requested planet-and-moon model for lower load and better transitions.
- **Fix/Notes:** Moon stagger is computed as `min(90ms, 300ms / count)`, so a larger moon system arrives in quicker succession rather than taking proportionally longer — verified as 60ms per step for Azure's five moons and 90ms for Postgres' two. Moons are positioned with `rotate(axis) translateX() rotate(-axis)` so they travel out along their own axis while the glyph stays upright, and the whole sequence is CSS transitions driven by a per-moon `--moon-delay`, with no JavaScript in the hover path. Rings pause while a planet is open because hovering a moving target is unpleasant and the moons need to be readable once they arrive. That made a latent bug in the vendored component load-bearing: `paused` previously tore the animation down and recreated it, which would have restarted from the motion value's current position and run to 100 over the full duration, corrupting both speed and loop range on every hover. The animation is now created once and paused/played through its controls, verified to resume and keep moving. An open planet is lifted above the other orbits with `:has()` on the ancestors, since each `.orbit-item` is its own stacking context. The planet label sits 3.3rem below the planet box to clear the 62px moon orbit, verified as no overlap for both the densest and sparsest systems. Several near-black brand colours (Copilot, Next.js, Express, Notion, GitHub, Pandas, NumPy) were lightened, as they were invisible against the black surface. The screen-reader inventory is regrouped by planet so the accessible structure matches the visual one, still covering all 36 skills. Verified in production: 8 planets, 28 moons, 8 animated items, 36 list items, zero page errors, reduced motion stationary with delays dropped, mobile unchanged as list-only with no horizontal overflow.

#### [FEAT] Seven single-planet orbits, hover focus, and turning moon systems
- **Template/File:** `data/skillsData.jsx`, `components/skills/SkillOrbits.jsx`, `components/skills/OrbitImages.jsx`, `app/globals.css`
- **Record:** `skills-seven-orbits-hover-focus`
- **What changed:** Every planet now has an orbit to itself: seven planets on seven rings, all running the same way round with periods that lengthen with distance. Hovering a planet blurs and dims the rest of the system, slows the other orbits to 0.3x instead of stopping everything, and sets the open planet's moon system turning around it.
- **Before:** Eight planets shared four rings, two to a ring, alternating direction; hovering froze the whole system and the moons were static once revealed.
- **After:** One planet per orbit as in an actual solar system, with the inspected planet held still and sharp while everything else recedes and drifts.
- **Why:** Requested; two planets sharing an orbit is not how a solar system works.
- **Fix/Notes:** Going to one item per ring exposed a problem: a ring carrying a single item always starts at 0% of its path, so all seven would have begun stacked on one side. Added a `phase` prop to the vendored component, folded into the existing item-offset modulo, and spread the seven starting positions — verified at 6/46/81/19/62/32/90. Slowing the other rings could not be done by changing `duration`, which would recreate the animation and restart it from the current position; a `speed` prop was added instead and applied to the animation controls' playback rate, measured at 0.29 against an unhovered baseline. The moon spin is a CSS animation on the moon container with an equal counter-rotation on each moon body, so the glyphs stay upright while the system turns; that meant the moon hover could no longer use `transform`, since an animation overrides it, so the hover now scales the glyph inside and changes the chip's border and background. Dropping from eight planets to seven meant retiring Postgres as a planet: TypeScript absorbed the backend and data stack (JS, Express, Prisma, Postgres, Redis) and HTML moved to React. All 36 skills still appear exactly once, verified programmatically for count and duplicates. Stage height went from 540 to 680 so an open planet's moons and label clear the top and bottom of the outermost orbit. The blur is scoped to at most six small elements and only while hovering, so it is not a filter on a large scrolling surface. Verified in production: 7 rings with exactly one planet each, 29 moons, 36 list items, others blurred on hover and cleanly restored on leave, moon system confirmed turning, reduced motion leaves orbits stationary with the spin disabled, mobile unchanged as list-only with no horizontal overflow, zero page errors.

#### [FIX] Planet layering, hover reach, planet icon set, and centre label
- **Template/File:** `data/skillsData.jsx`, `components/skills/SkillOrbits.jsx`, `components/Skills.tsx`, `app/globals.css`
- **Record:** `skills-planet-layering-and-icons`
- **What changed:** Planets now always paint above the orbit lines. A circular hover pad keeps a planet open while the pointer crosses the gap between its core and its moons. The planet icon set was reworked: Azure, Docker and GitHub kept; TypeScript, Python, ChatGPT and React retired as planets in favour of category marks — Power Platform, AI, Code and Web. The "Cloud and AI" label was removed from the centre, leaving the glow.
- **Before:** Each ring drew its own orbit path, so an outer ring's line painted over an inner ring's planet. Moving from a planet to its moons crossed dead space and closed the group. Product logos stood in for whole disciplines.
- **After:** One shared path layer beneath all planets; an unbroken hover region; category planets that describe the discipline rather than one product in it.
- **Why:** Reported layering, hover-loss and icon-fit problems.
- **Fix/Notes:** The layering fault was a stacking-context one: `.orbit-rotation-wrapper` carries a `transform`, so each ring is its own stacking context and a planet inside ring 0 cannot rise above ring 6's path however it is z-indexed. Fixed by not drawing paths per ring at all — `showPath` is off and SkillOrbits renders all seven ellipses in a single SVG beneath the rings, which also replaces seven SVG elements with one. The hover pad is a 184px circle inside `.planet`, reaching past the 62px moon orbit plus a moon radius; it is `pointer-events: none` until the planet opens, so it cannot enlarge a closed planet's hit area. Verified the group stays open across the whole dead zone at 42, 52, 70 and 86px from centre and closes only on genuinely leaving. Testing surfaced a second issue: because the moon system rotates, a moon can drift under a stationary pointer, and its tooltip then lands on top of the planet label. The planet name now gives way while a moon is being read. Four of the seven planets are category marks rather than skills, so `isSkill` distinguishes them and the screen-reader list omits the labels — still exactly 36 skills. Several brand colours were lightened again for legibility on black. Tested throughout with ego-browser; note that its `captureScreenshot` helper returned an 8KB black frame while the page was rendering normally, so verification screenshots were taken through `cdp('Page.captureScreenshot')` instead. Verified in production: 7 planets one per ring, 7 paths in a single shared layer with 0 per-ring paths, 33 moons, 7 reach pads, 36 list items, centre label empty.

#### [FIX] AI moon set, and remove orbit position jumps on hover handover
- **Template/File:** `data/skillsData.jsx`, `components/skills/OrbitImages.jsx`, `components/skills/SkillOrbits.jsx`, `app/globals.css`
- **Record:** `skills-ai-moons-and-orbit-jump`
- **What changed:** The AI planet now carries Claude Code, OpenCode, HuggingFace, LangChain and n8n; OpenAI and TensorFlow were dropped. Planets no longer jump along their orbit when hover moves from one planet to another.
- **Before:** AI held OpenAI, HuggingFace, LangChain, TensorFlow and n8n. Switching hover between planets could visibly displace a planet on its path.
- **After:** The requested AI set, and a frame driver in which a positional jump is not expressible.
- **Why:** Requested icon change, plus a reported jump on hover handover.
- **Fix/Notes:** The jump had two independent causes, both found by measurement rather than inspection. First, `openId` dropped to null in the gap between leaving one planet and entering the next, so every orbit briefly returned to 1x before slowing again — the released planet visibly darted away. Closing is now deferred by 180ms and cancelled if another planet opens, so the system never passes back through full speed. Second, and more fundamental: pausing, resuming or re-rating a framer-motion tween all re-derive its start time, which seeks the playhead. Instrumenting every frame showed single-frame jumps up to 9.3% of an orbit — far beyond the ~0.1% of real drift. Preserving `controls.time` across the `speed` assignment fixed the rate case but not `pause()`/`play()`. The tween was therefore replaced with a hand-driven frame loop that integrates `dt * rate`: progress can only advance by elapsed time, so no seek is representable. Rate and direction are read from refs so changing them never restarts the loop, and the clock is re-seeded on resume so a pause cannot accumulate into a catch-up leap. Measured afterwards: held rings drift exactly 0.0000%, and a released ring resumes with no leap. A third bug surfaced while testing: the Azure planet could not be hovered at all, failing six consecutive attempts, because the decorative centre glow was intercepting pointer events; it is now `pointer-events: none` and all seven planets are reachable. OpenCode has no react-icons glyph and no local asset, so a terminal mark stands in rather than hand-rolling a fake logo — drop an SVG into /public/icons and set `img` to swap it. Claude Code uses the existing /icons/claudecode-color.svg. Skill count is unchanged at 36 (two removed, two added), verified in production along with the AI moon list. Reduced motion leaves orbits stationary; mobile remains list-only with no horizontal overflow.

#### [FIX] Fixed spacing between planets across the whole cycle
- **Template/File:** `components/skills/SkillOrbits.jsx`
- **Record:** `skills-static-planet-spacing`
- **What changed:** All seven orbits now share one period (64s) instead of periods lengthening with distance, and every orbit freezes together while any planet is hovered.
- **Before:** Periods ran 24s to 93s, so planets swept through every alignment over time and could bunch up or overlap, making hover targets unreliable. Hover also slowed only the non-held rings, which let separations drift during a hover session.
- **After:** The arclength gap between any two planets is invariant forever, and nothing moves while a planet is open.
- **Why:** Requested static spacing so each planet is reliably hoverable.
- **Fix/Notes:** Equal periods make separation invariant because all planets traverse the same path shape at the same arclength rate. That alone was not enough: with one period, relative positions are determined entirely by the starting phases, and the previous hand-picked phases produced a worst-case edge-to-edge clearance of about 22px somewhere in the cycle. The phases were therefore chosen by seeded search over arclength-space positions of the flattened ellipses (eccentric-anomaly tables with cumulative arc length, binary-search inversion, tilt applied afterwards), then hill-climbed: worst-case clearance 34px against a hard ceiling of roughly 10px when adjacent rings align, since the radial gap between rings is ~73px and the largest glyph pair sums to ~67px. Freezing all rings on hover rather than only the inspected one preserves those separations through any number of hover sessions, which the earlier per-ring slowdown quietly broke. Measured in the live page over a ~12s window: minimum centre-to-centre distance between any two planets 125.5px, against 55px needed to touch. All seven planets individually hoverable and pixel-frozen while open; pairwise spacing byte-identical before and after hovering three planets. Trade-off worth noting: planets no longer move at Kepler-varying speeds, so the system loses that touch of realism in exchange for deterministic geometry.

#### [FEAT] Moons visible as revolving dots that morph into icons on hover
- **Template/File:** `app/globals.css`, `components/skills/SkillOrbits.jsx`
- **Record:** `skills-moon-dots`
- **What changed:** A closed planet no longer hides its moons. Each moon is now a small dot revolving around its planet at all times, on a tight orbit just outside the planet glyph; hovering the planet sends the dots out to the full 62px orbit while each dot cross-fades into its icon, with the existing stagger. Leaving reverses the morph back to dots.
- **Before:** Moons were invisible until hover (`opacity: 0`, parked at the planet centre), so a closed planet gave no hint that it carried anything.
- **After:** Every planet shows a revolving constellation of dots — 33 across the system — that advertise the moons before any interaction.
- **Why:** Requested: show the moons as always-revolving dots that animate into the icons on hover.
- **Fix/Notes:** The revolution (container spin plus per-body counter-rotation, 18s) moved from hover-gated to mount-time. That pairing is load-bearing: both animations must start on the same frame so they cancel exactly, which is what keeps icons upright when the dots morph in — gating one of them on hover would have icons arriving tilted by the container's current angle. The dot is a `::before` on `.moon-body`, stacked in the same grid cell as the glyph (`grid-area: 1/1`) so the two cross-fade in place; the closed-state orbit radius is published per planet as `--dot-orbit` (size/2 + 10px), which keeps dots clear of the planet glyph at every planet size and, at the worst-case planet alignment, still leaves neighbouring systems' dots ~9px apart (protrusion 12.5px each against the 34px minimum planet clearance). Closed moons are `pointer-events: none` so a dot 40px from the core cannot open a planet in passing; they become hoverable only once open. The 40 always-running CSS animations pause via `animation-play-state` when the section reports `data-animating="false"` (off-screen), and reduced motion removes them entirely, leaving static dots with instant morphs. Verified with ego-browser against the running dev server: 33 dots visible and revolving at idle with all glyphs hidden, dot orbit measured at 48px screen vs a 35px planet half-width, open state shows all five AI moons at a uniform 80px screen radius with dots gone and bodies hoverable, morph reverses on leave, and reduced motion reports `animation: none` with dots still visible. One measurement artifact worth recording: an ego-browser `hover()` teleport to a far corner did not immediately produce the mouseout React needed, so a same-heredoc read showed the planet still open; the next event flushed it and the state read correctly — real pointers move continuously and are unaffected. Lint 0 errors; isolated production build passed without touching the running server or the project's `.next`.

#### [FEAT] Replace the centre gradient with the summer-sun image
- **Template/File:** `components/skills/SkillOrbits.jsx`, `app/globals.css`, `public/icons/summer-sun.png`
- **Record:** `skills-sun-image`
- **What changed:** The orange radial-gradient glow at the centre of the skills solar system is replaced by the supplied cartoon sun (spiky halftone body, sunglasses), rendered at 150px design width.
- **Before:** A soft translucent radial gradient stood in for the star.
- **After:** The summer-sun image is the star.
- **Why:** Requested swap to the attached sun image.
- **Fix/Notes:** The supplied file was located at ~/Downloads/summer-sun.gif — a 6-frame animated GIF with native transparency (200x150), not a PNG; frame 0 was exported via Pillow to public/icons/summer-sun.png (2.4KB, 59% opaque) since a static image was requested and a perpetually animating centre would need motivation plus a reduced-motion path. The swap forced one structural change: the old glow was translucent and painted above the rings harmlessly, but a solid sun there would swallow the innermost planet for much of its revolution, because that orbit's vertical radius (36px) is smaller than the sun's half-height (~57px). The core is therefore rendered before the rings in the stage, so planets always pass in front of the sun. The core keeps pointer-events: none — the earlier Azure-unhoverable bug was exactly this element intercepting hovers. Verified against the running dev server: image loaded at natural 200px width and drawn at 193x145 screen, gradient element gone, core inert and DOM-ordered before the rings, and Azure hoverable while passing directly beside the sun. Lint 0 errors; isolated production build passed; the user's dev server on 3100 was untouched. The source GIF's other five frames are unused — say the word if the sun should animate instead.

#### [FIX] Bring the animated sun GIF above the orbit lines
- **Template/File:** `components/skills/SkillOrbits.jsx`, `app/globals.css`, `public/icons/summer-sun.gif`
- **Record:** `skills-sun-gif-front`
- **What changed:** Replaced the static exported PNG with the original animated `summer-sun.gif` and moved the sun layer after the planet rings with `z-index: 10`, so the sun paints in front of the orbit paths.
- **Before:** A static `summer-sun.png` sat beneath the rings so planets could pass in front of it.
- **After:** The original animated GIF is rendered above the paths. The core remains `pointer-events: none`, so it cannot block planet hover.
- **Why:** Requested the sun in front and as a GIF rather than a PNG.
- **Fix/Notes:** The source asset was already available at `~/Downloads/summer-sun.gif`, a transparent 6-frame 200x150 GIF. The generated PNG remains in the working tree from the previous request but is no longer referenced by the application; it was not deleted to preserve existing work under the repository's no-silent-deletion rule. The sun's stage order is now paths -> planet rings -> sun, and its `z-index: 10` is above the ring stack contexts. The earlier pointer safety is preserved with `pointer-events: none` on the core. Verified through ego-browser against the user's running server on port 3100: `src=/icons/summer-sun.gif`, natural dimensions 200x150, core z-index 10, core pointer-events none, Azure still opens, and two CDP compositor captures 800ms apart differ (animated frames confirmed). Isolated production build passed; lint reports 0 errors and the user's dev server was not interrupted.

#### [FEAT] Spread inner orbit angles and reduce the centre sun
- **Template/File:** `components/skills/SkillOrbits.jsx`, `app/globals.css`
- **Record:** `skills-orbit-angle-spread`
- **What changed:** Each of the seven orbital planes now has its own tilt: inner rings use stronger opposing angles (-28°, 22°, -18°, 14°), while outer rings progressively flatten (-10°, 8°, -5°). The animated sun was reduced from 150px to 118px design width.
- **Before:** All ring paths used one shared -18° effective rotation, compressing the inner planets around the centre sun and hiding them intermittently.
- **After:** Inner planets have more vertical clearance from the sun, outer rings retain the broad skewed-plane silhouette, and the smaller sun occupies less of the inner system.
- **Why:** Requested more angle separation and a smaller centre image so inner planets remain visible.
- **Fix/Notes:** Path and planet transforms use the same per-ring angle (`cfg.tilt + TILT`), so every planet remains attached to its own path. The first phase set was retained temporarily and measured live; it produced an actual sampled overlap of -5.4px because those phases had been optimized for one shared plane. Rather than hiding that with z-index or larger hover pads, phases were re-optimized against the new tilted geometry and the ring spacing was expanded to 125/220/315/410/505/595/675. The live frame then measured 92.6px minimum edge clearance, and all seven planets remained individually hoverable. The sun remains above the orbit paths, pointer-inert, and the GIF animation remains unchanged. Verified with ego-browser: 7 rings, 7 planets, 7 shared paths, all seven hover targets opening successfully, inner planets no longer hidden by the reduced sun, reduced-motion animations disabled, mobile list-only with 36 skills and no horizontal overflow. Isolated production build passed without touching the user's server on port 3100.

#### [REFACTOR] Simplify skills solar system to four wide rings
- **Template/File:** `components/skills/SkillOrbits.jsx`, `data/skillsData.jsx`
- **Record:** `skills-four-wide-rings`
- **What changed:** Reconfigured the seven planets across four wider rings as 2/2/2/1, reduced the per-ring angle spread to a subtle -12°/-6°/5°/10°, widened the radii to 270/405/540/670, and increased the stage height to 700 design pixels.
- **Before:** Seven separately tilted rings at 125/220/315/410/505/595/675 created too much skew and brought the inner system too close to the sun.
- **After:** Four calmer, wider rings with planets grouped opposite each other on shared orbits; the first ring is pushed out far enough to give the sun and expanded moon systems breathing room.
- **Why:** Requested a simpler four-ring layout because the seven-plane structure was too skewed and inner rings were hiding against the sun.
- **Fix/Notes:** `OrbitImages` already distributes two items evenly at 50% path distance, so the first two pairs share each ring at opposite positions and never cluster within their own orbit. Data ring assignments are now Azure/Power Platform (ring 0), AI/Code (ring 1), Docker/Web (ring 2), GitHub (ring 3). A first pass at four rings only changed the angle distribution and was measured live at -3px planet-to-sun clearance for the inner pair; rather than relying on layer order or hover hit padding, the first ring moved to 270px and the next rings to 405/540/670px. The resulting live frame measured 22.1px inner planet-to-sun edge clearance and 103.7px for the next pair. The sun remains 118px design width and in front of paths. Verified using ego-browser against the user's running server: exactly 4 rings, 7 planets with per-ring distribution [2,2,2,1], all previous planet names present and hoverable, reduced motion disables orbit animation, mobile stays list-only with all 36 skills and no horizontal overflow. Isolated production build passed; port 3100 was not interrupted.

#### [REFACTOR] Use four separated electron-like shells to prevent overlap
- **Template/File:** `components/skills/SkillOrbits.jsx`, `data/skillsData.jsx`
- **Record:** `skills-four-shell-spacing`
- **What changed:** Consolidated the seven rings into four shells with planets distributed 2/2/2/1, restored subtle shared tilt, and changed each shell to explicit vertical radii: 260x180, 420x300, 550x420, and 670x540. All shells share the same 64s clock and phase pattern so their relative shell positions remain stable.
- **Before:** Four shells with very flat, similar vertical radii and multiple phase/speed experiments caused screen-space overlaps as the shells revolved; a 40-sample live measurement recorded -37.2px edge clearance.
- **After:** Four visibly separated electron-like shells with enough vertical breathing room for planets and moon envelopes; the same live sampling measured 73.3px minimum planet edge clearance.
- **Why:** Requested four rings far enough apart that planet and moon systems do not overlap the sun or one another.
- **Fix/Notes:** Different shell speeds were tested as a possible electron analogy but rejected as the collision-safe mechanism: differing periods eventually produce every relative alignment, so no fixed speed choice can guarantee non-overlap. A shared 64s clock was retained while shell geometry was widened; the important correction was explicit vertical separation, because the previous flattened ellipses had neighbouring shell centres only ~35px apart at their top/bottom points while planet diameters were 54-68px. The first widened pass still measured -14.7px because its vertical radii remained too compressed; the final radii increase the design stage to 1120 and produce 73.3px minimum live edge clearance across 20 samples, with a 73px+ target margin over the largest planet pair. The full planet/moon hover behaviour remains unchanged, and the sun stays clear of the inner shell. Verified through ego-browser against the user's running server: 4 rings, per-ring planet distribution [2,2,2,1], no observed overlap in the extended live sample, all prior planet hover targets still operate, reduced-motion and mobile paths remain supported. Lint 0 errors and isolated production build passed; port 3100 was not interrupted.

#### [FIX] Fit the four-shell skills system within one viewport
- **Template/File:** `components/skills/SkillOrbits.jsx`
- **Record:** `skills-compact-four-shell-stage`
- **What changed:** Reduced the skills design stage from 1120 to 620 design pixels and tightened the four shell bounds to 210x120, 340x190, 470x260 and 600x330. The Skills section now measures 968px within a 1008px ego-browser viewport instead of exceeding one page height.
- **Before:** The previous safety spacing used a 1120px design stage, which scaled with viewport width to roughly 1420px on a 1784px browser and pushed the skills section beyond one viewport.
- **After:** The four-shell layout fits within one desktop viewport while retaining visible separation between the sun, planets and moon dots.
- **Why:** Reported that the skills section no longer fit in one page height.
- **Fix/Notes:** The previous fix optimized collision clearance but treated design-space height as CSS pixels; SkillOrbits scales the 1400px design coordinate system by container width, so a 1120px stage became much taller on wide screens. The compact stage returns to 620 design pixels and uses proportionally nested vertical radii. At the verified 1008px viewport, the complete section is 968px high, with the orbit wrapper 797px; the centre sun remains clear of the inner planets in the captured frame, and the layout has no horizontal overflow. The four rings remain distributed 2/2/2/1, with the shared clock and hover-freeze behavior unchanged. Verified via ego-browser: AI planet opens, reduced motion reports no orbit animation, mobile remains list-only with all 36 skills and no overflow. Lint reports 0 errors and the isolated production build passed; the user's port 3100 server was not interrupted.

#### [FIX] Scatter planets and moon dots across four shells
- **Template/File:** `components/skills/SkillOrbits.jsx`
- **Record:** `skills-scattered-four-shells`
- **What changed:** Offset the four shell phases from `0/0/0/0` to `0/24/52/77`, keeping the two planets on each shared ring opposite while distributing each shell around the sun.
- **Before:** Every shell used phase 0, so corresponding planets and their moon dots lined up into a single diagonal/column.
- **After:** Planets and dot constellations are visibly scattered around the system while preserving the wide nested shells and the stable shared clock.
- **Why:** Requested that the icons not appear in a line.
- **Fix/Notes:** A live ego-browser measurement after the phase change recorded 124.9px minimum planet edge clearance in the captured frame, with no overlap. The four-ring structure remains 2/2/2/1, the sun remains centred, moon hover behavior is unchanged, and all previous hover targets remain available. Lint reports 0 errors and the isolated production build passed; the user's port 3100 server was not interrupted.

#### [FEAT] Replace contact form and footer graphics with a direct conversation panel
- **Template/File:** `components/Contact.jsx`, `components/SiteFooter.jsx`, `app/layout.tsx`, `app/globals.css`
- **Record:** `contact-panel-and-footer-removal`
- **What changed:** Replaced the full contact grid/form/status workflow with a reference-style editorial panel: paper background, vertical guide line, cobalt-blue 70%-width panel, bracketed “Start a conversation” heading, divider, and direct `mailto:abhinavyadav8@gmail.com` action. Removed the footer entirely from the root layout, including its micrographics SVG and minimal footer bar.
- **Before:** Contact showed social links, response time, a form with name/organization/email/message fields, submit states, and a TALK watermark; SiteFooter rendered a large deterministic technical micrographics field.
- **After:** Contact is a single direct-email CTA; no footer is rendered.
- **Why:** Requested the supplied visual direction and removal of footer graphics; requested email replacement.
- **Fix/Notes:** The existing `/api/contact` workflow is no longer used by the public contact surface because the form was explicitly removed. The `#contact` anchor remains intact. The main CTA and inline email both use `mailto:abhinavyadav8@gmail.com`. Desktop panel width is 70% of its containing section, with a `white-space: nowrap` heading to match the reference; mobile switches to full width and allows the heading to wrap. The visual footer component was not deleted to preserve existing files under the repository's no-silent-deletion rule, but it is no longer imported or rendered by `app/layout.tsx`. Verified with ego-browser against the user's running server: desktop panel measured 1103px in a 1800px viewport (~61% of viewport because of the section's horizontal padding, 70% of its containing content box), heading one line, footer absent, form absent, correct mailto; mobile panel measured 364px in a 390px viewport, no horizontal overflow, correct mailto, and footer absent. Lint reports 0 errors and isolated production build passed; port 3100 was not interrupted.

#### [FEAT] Skills sun tooltip and expanding black contact panel
- **Template/File:** `components/Skills.tsx`, `components/skills/SkillOrbits.jsx`, `components/Contact.jsx`, `app/globals.css`, `app/layout.tsx`
- **Record:** `skills-sun-tip-contact-expansion`
- **What changed:** Removed the visible “Capabilities / Skills” header from the skills section, added the “Cool skills” tooltip to the centre sun, and changed Contact to a black section with a compact blue panel that expands as it enters the viewport. The footer remains removed.
- **Before:** Skills showed the two-line section header and a centre sun with no label; Contact used a large paper-backed panel that was fully sized immediately.
- **After:** The skills area opens directly into the solar system; hovering the sun reveals “Cool skills”; Contact begins as a small box and scales to the full reference panel on scroll.
- **Why:** Requested lower visual density in Skills and a black, scroll-revealed contact treatment.
- **Fix/Notes:** Contact uses an isolated client GSAP leaf with a `top 92%` to `top 28%` ScrollTrigger and a reduced-motion immediate state. Desktop panel width remains 70% of the content box; mobile becomes full width with no horizontal overflow. `SiteFooter` is no longer imported by the root layout, so no footer graphics or footer bar render. The sun remains the supplied animated GIF, now with `pointer-events: auto` so its tooltip can be hovered, while its inner image remains decorative and the existing planet hover paths continue to work. The initial live check found the contact panel at scale 0.48 during entry; a later sampling command used a stale scroll state and returned scale 1 at every point, so the final implementation was verified through the visible panel and build path rather than treating that stale sequence as evidence. Verified with ego-browser: Skills header absent, “Cool skills” present, sun source `/icons/summer-sun.gif`, black contact background, form/footer absent, direct email retained, desktop and mobile layout valid. Lint reports 0 errors; isolated production build passed; user's port 3100 was not interrupted.

#### [FIX] Make contact box travel from the top before expanding at centre
- **Template/File:** `components/Contact.jsx`, `app/globals.css`
- **Record:** `contact-top-to-centre-expand`
- **What changed:** Contact now owns a full viewport-height black stage. Its compact blue box begins above the stage, travels down to the vertical centre, then expands in place to the full panel size.
- **Before:** The panel was in normal flow and the scrubbed scale animation often started after the section had already entered, making the box appear fully expanded immediately.
- **After:** The box enters from above at 0.34 scale, reaches centre during the first 55% of the scroll range, and expands during the final 45%.
- **Why:** Requested top-to-middle travel followed by expansion.
- **Fix/Notes:** The initial implementation used `yPercent` on an in-flow panel, but the panel's containing geometry meant the travel was not visually tied to the section centre. The panel is now absolutely centred on the section with a dedicated 100dvh stage, and GSAP animates to `section.clientHeight / 2 - panel.offsetHeight / 2`, so its landing point is geometrically the section midpoint. The reveal trigger uses `top 98%` to `top 18%`; reduced motion skips travel and expansion animation and shows the complete panel immediately. Verified with ego-browser against the user's running server that the compact state is `scale 0.34` with the panel translated above its centre, and the panel reaches full scale after the staged scroll. The earlier probe using `scrollIntoView()` intentionally landed at the end of the trigger and was not used as evidence for intermediate states. Lint reports 0 errors and isolated production build passed; port 3100 was not interrupted.

#### [FIX] Compact skills viewport, bottom-anchored contact, and intro scroll lock
- **Template/File:** `components/skills/SkillOrbits.jsx`, `components/Contact.jsx`, `app/globals.css`
- **Record:** `compact-skills-contact-bottom-intro-lock`
- **What changed:** Compressed the four shell radii/stage so the outer ring is no longer clipped, changed Contact from a full-height section to a content-sized black stage with the expanded panel anchored at its bottom edge, and locked document scrolling while the intro load choreography is active.
- **Before:** The skills design stage scaled beyond the viewport and clipped the outer shell; Contact occupied a full viewport-height section; users could scroll behind the intro animation.
- **After:** Skills fits within the viewport with 100.3px measured minimum planet clearance; Contact is a 34rem desktop / viewport-aware mobile stage whose panel lands on the bottom edge after its top-to-centre-to-expand sequence; `html.intro-running` sets both document and body overflow hidden before first paint and releases after the intro.
- **Why:** Requested fit, bottom termination, and intro scroll behavior.
- **Fix/Notes:** Skills stage changed from 620 to 510 design pixels and shell vertical radii from 120/190/260/330 to 88/138/188/238, retaining horizontal radii 210/340/470/600 and the scatter phases. The four-ring layout remains 2/2/2/1. Contact keeps its compact start but now uses `bottom: 0`; its first animation segment translates by `-(section.clientHeight - panel.offsetHeight)/2`, placing the compact panel centre at the section midpoint, and its second segment returns `y: 0` while scaling to 1, so the final panel bottom is exactly the section bottom rather than leaving an extra full-height tail. The stage remains `100dvh` on mobile for safe touch spacing and full-width panel behavior. Intro scroll lock uses the pre-paint class already set by the layout inline script, so wheel/touch/keyboard scrolling cannot move behind the greeting; the existing failsafe removes the lock after 9 seconds, and reduced motion releases it immediately. Verified with ego-browser against the user's running server: fresh load reports `scrollY=0`, html/body overflow hidden and `intro-running=true`; after the sequence both overflow values are visible and the class is false; skills orbit height 656px and section height 742px in the 1008px viewport, 4 rings, no horizontal overflow, minimum live planet clearance 100.3px; Contact stage is 656px, and production isolated build passed. Lint reports 0 errors; port 3100 was not interrupted.

#### [FIX] Keep skills orbit and contact panel within the viewport
- **Template/File:** `components/skills/SkillOrbits.jsx`, `app/globals.css`, `components/Contact.jsx`
- **Record:** `skills-orbit-viewport-and-contact-bottom`
- **What changed:** Reduced the Skills orbit stage from 510 to 400 design pixels and tightened shell vertical radii to 58/96/134/172, keeping the outer ring inside the viewport. Contact now uses a 34rem desktop / 28rem mobile content-sized stage with a bottom-anchored panel, rather than a full-height section.
- **Before:** The outer shell and moon dots were cut off at scroll end; Contact occupied a full viewport height and could leave the panel visually cut during the staged entrance.
- **After:** Skills orbit measured 514px in the 1008px ego-browser viewport with no horizontal overflow. Contact ends with the panel attached to the bottom of its compact section.
- **Why:** Requested an in-viewport orbit and a contact section that ends at the page bottom rather than extending as a full-height block.
- **Fix/Notes:** The orbit stage is scaled from the 1400px design coordinate space, so every design-space height is multiplied by viewport/container width; the previous 510px stage was still too tall in the active browser. The new 400px stage plus section heading measures 514px in the tested viewport. Contact's panel remains absolutely positioned at `bottom: 0`; its ScrollTrigger first moves the compact panel from above to the section midpoint, then returns `y` to 0 while scaling to its final size, so the final box bottom and section bottom share the same edge. The contact section is now fixed at 34rem desktop and 28rem mobile rather than `100dvh`, with the stage filling that section. Intro scroll locking is unchanged and remains governed by the pre-paint `intro-running` class. Verified using ego-browser against the user's running port 3100 server: Skills orbit top 46px/bottom 560px inside a 1008px viewport, Contact stage 544px and panel bottom 917px while its scroll reveal was in progress, and the production isolated build passed. Lint reports 0 errors; port 3100 was not interrupted.

#### [FIX] Keep contact form expansion inside the page bounds
- **Template/File:** `components/Contact.jsx`, `app/globals.css`
- **Record:** `contact-form-expand-bottom-bound`
- **What changed:** Fixed the transformed contact panel's bottom positioning and added click-to-expand form behavior. Clicking `[ Start a conversation ]` expands the card, reveals Name, Organization, Email and Message fields, hides the direct-email row, and submits through the existing `/api/contact` contract.
- **Before:** The contact panel could extend beyond the viewport at full-page scroll, and the conversation CTA only linked directly to email.
- **After:** The panel stays within the contact section's bounds, and the CTA toggles an inline contact form. Form mode removes “or write directly” and retains the requested email only as the form's error fallback copy.
- **Why:** Requested the panel remain visible at scroll end and become an expandable contact form on click.
- **Fix/Notes:** The panel's original bottom anchor plus `scale(0.34)` meant a final `y: 0` positioned the transformed box above the actual section bottom. The expansion timeline now computes the final y offset from the initial scale so the unscaled panel's visual bottom lands on the section bottom. Form state is client-local; required field names remain `name`, `email`, and `message`, with optional `organization`; submission preserves the existing API payload keys `firstname`, `organization`, `email`, `message`, and `to`. The heading is a semantic button with `aria-expanded`, and the fields use labels, autocomplete, required constraints, focus styles, loading state and status messaging. Verified with ego-browser: compact panel, form expansion, direct-email row absent in form mode, four expected fields, correct API-ready names, and no mobile horizontal overflow. Lint reports 0 errors and isolated production build passed; port 3100 was not interrupted.

#### [FIX] Bound expanded contact form to the contact section
- **Template/File:** `components/Contact.jsx`, `app/globals.css`
- **Record:** `contact-form-bottom-bound`
- **What changed:** Changed the compact contact panel's animation anchors to explicit top-to-centre-to-bottom geometry, and re-anchor the panel after form expansion so the taller form grows upward instead of extending below the section.
- **Before:** The compact panel could appear cut at the page edge, and opening the form changed its intrinsic height after the scroll animation measured it, causing the expanded card to extend past the contact section.
- **After:** The panel starts at the top anchor, travels to the section midpoint, expands, and remains inside the section; the expanded panel's bottom edge is measured inside the contact section.
- **Why:** Requested that the contact box end at the exact reference position without moving or expanding further, and keep the click-open form within the page.
- **Fix/Notes:** The prior animation mixed a bottom CSS anchor with a negative/positive `y` sequence, so the visual transform and layout anchor could disagree. The panel is now `top: 0`, starts at `y: 0` with `scale: 0.34`, travels to `(sectionHeight - panelHeight) / 2`, then finishes at `y: sectionHeight - panelHeight` while scaling to 1. Clicking the CTA changes panel content and therefore changes `offsetHeight`; a `requestAnimationFrame` effect recalculates the final bottom-anchor offset after React commits the form, growing the card upward and keeping its bottom inside the section. Verified with ego-browser: normal panel section `[336,1008]`, panel `[543,988]`; after opening the form panel `[332,990]`, `inside: true`, form present, direct-email row absent. Lint reports 0 errors and the isolated production build passed; user's port 3100 was not interrupted.

#### [FIX] Bound contact marker-to-panel morph and prevent form overflow
- **Template/File:** `components/Contact.jsx`, `app/globals.css`
- **Record:** `contact-marker-final-size`
- **What changed:** Replaced the scaled-down full panel entrance with a distinct 12px blue square that travels down the contact guide, then expands into a bounded panel. The final desktop panel width is 38% of the content box. Form mode now forces scale 1 and recalculates the bottom offset after the form's intrinsic height changes.
- **Before:** The panel itself was scaled from above, had no visible marker phase, could overshoot its intended final dimensions, and opening the form could extend beyond the contact section.
- **After:** The square travels first, disappears as the panel grows, and the final panel stays within the section. Form mode hides the direct-email row and remains inside the section bottom.
- **Why:** Requested the small blue square traveling down the vertical line and a strict final panel size.
- **Fix/Notes:** The contact timeline now animates the marker separately: marker starts at the guide top, travels to `(sectionHeight - 12) / 2`, fades, then the fixed-size panel scales from 0 to 1 at its final bottom target. This prevents scale and translation from compounding into an oversized/misplaced final panel. The panel was moved to `top: 0` so the marker and panel share a predictable coordinate system. Opening the form changes panel height after the initial GSAP measurement; a `requestAnimationFrame` effect now sets `scale: 1` and `y: sectionHeight - panel.offsetHeight`, making the form grow upward while keeping its bottom in bounds. Verified with ego-browser: marker is 12px and begins at guide x, normal panel remains within the contact section, form mode has four fields, direct-email row absent, and final form panel bottom equals section bottom (1008px) with `inside: true`. Lint reports 0 errors and the isolated production build passed; user's port 3100 was not interrupted.

#### [FIX] Contact animation froze at 0.897 scale; panel now completes and grows from the marker
- **Template/File:** `components/Contact.jsx`, `app/globals.css`
- **Record:** `contact-scrub-freeze`
- **What changed:** Switched the contact ScrollTrigger from `scrub: 0.7` to `scrub: true`, replaced the document-tail `end` with a fixed `+= 60%` of section height, set the panel's `transform-origin` to the marker's landing point, and removed a stale hardcoded `transform: scale(0.34)` plus a duplicated `transform-origin` from `.contact-panel`.
- **Before:** The panel permanently stopped at `scale 0.897 / opacity 0.9` and inflated from its own centre, unrelated to the blue square.
- **After:** The square travels the guide, fades, and the panel expands from that exact point to `scale 1 / opacity 1`, ending flush with the section bottom.
- **Why:** User reported the animation was still broken after the previous change.
- **Fix/Notes:** Diagnosed by sampling the timeline at scroll fractions rather than only endpoints. Three compounding faults: (1) the contact section is the last block on the page, so ends tied to the document tail (`top 18%`, then `bottom bottom`) were unreachable; (2) Lenis smooth scroll goes idle when input stops, so the smoothed scrub's catch-up tween stalled ~10% short — the identical `0.897` reading under every end config isolated the scrub, not the range; (3) `transform-origin: center center` plus the leftover CSS scale made the panel inflate from its own centre. Note: `openOrReuseTab` reuses the tab without reloading, which masked the earlier edits and produced byte-identical stale measurements; an explicit `location.reload()` was required. Verified after reload: progress runs 0 -> 1 with scale reaching exactly 1, final panel 684x432 with bottom flush to section bottom 1008 and `inside: true`; form open is 684x597, still inside, four fields present, direct-email row hidden; closing restores 684x432 and the direct-email row. Lint 0 errors / 13 warnings (baseline), isolated production build compiled successfully, port 3100 untouched.

#### [FIX] Contact square now rides the guide line and the panel unfolds from it
- **Template/File:** `components/Contact.jsx`, `app/globals.css`
- **Record:** `contact-marker-on-line`
- **What changed:** Moved `.contact-panel` left edge onto the guide (`34.5%` desktop, `22%` mobile), simplified the panel `transform-origin` to `0px halfHeight`, changed the ScrollTrigger to `start: "top 85%"` / `end: "max"`, and retimed the marker travel to begin at 15% with a linear ease over 60% of the timeline.
- **Before:** The square appeared already parked mid-section and the panel expanded away from the line.
- **After:** The square is on the line at every frame, descends in view the whole way, fades, and the panel unfolds from that point on the line to its full fixed size.
- **Why:** User reported the panel animating off the line and the square not travelling down it.
- **Fix/Notes:** Two separate faults. (1) With `start: "top 95%"` over a short fixed range the descent completed within the first ~222px of scroll while the section was still below the fold, so the travel was never seen; measurements confirmed the marker sat at y~1085 in a 1008px viewport mid-travel. Using `end: "max"` spends the remaining page scroll, and starting the marker at 15% with `ease: "none"` stops it outrunning the rising section. (2) The panel sat at `left: 28%` while the guide was at `34.5%`, so the expansion origin was a computed point floating inside the panel; pinning the panel's left edge to the guide makes `transform-origin: 0px` literally the line. Verified across 8 scroll fractions after an explicit reload: marker on-line and in-view at every step with a steady descent (779 -> 812 -> 847 -> 882 -> 916), panel left edge on the line at all scales, final 684x432 flush with the section bottom. Form open 684x597 on the line, inside, four fields. Mobile 430px: panel on the line at x=95, 322x260, no right overflow. Lint 0 errors / 13 warnings (baseline); isolated production build compiled; port 3100 untouched.

#### [FIX] Contact panel now expands from its centre, on the square
- **Template/File:** `components/Contact.jsx`, `app/globals.css`
- **Record:** `contact-expand-from-centre`
- **What changed:** Centred `.contact-panel` on the guide (`left: 15.5%` = 34.5% minus half the 38% width) and set `transform-origin: 50% 50%`. On mobile the guide and square moved to `50%` with the panel back to `left: 1rem` / `calc(100% - 2rem)`.
- **Before:** The panel's left border sat on the line and it unfolded rightward, so the square was at its edge rather than its middle.
- **After:** The square lands exactly at the panel's centre and the panel grows symmetrically around it.
- **Why:** User asked for the expansion to start from the middle of the box.
- **Fix/Notes:** Because the marker already lands at `finalY + panelHeight/2`, centring the panel horizontally on the guide makes the square's centre and the panel's centre the same point, and a `50% 50%` origin then grows the box outward from it. Mobile needed the guide moved from `22%` to `50%`: a full-width panel centred on a 22% guide would overflow off the left edge. Flagging that as a deliberate mobile visual change. Verified after reload across scroll fractions: square on the line at every step, and at the handoff (f=0.75) `centerDx: 0, centerDy: 0` — square centre exactly on panel centre — with `centerDx` staying 0 through scale 0.488 -> 0.936 -> 1, final 684x432 inside the section. Form open 684x597, centred, four fields. Mobile 430px: centreDx 0, 404x260, no overflow. Lint 0 errors / 13 warnings (baseline); isolated production build compiled; port 3100 untouched.

#### [FIX] Contact panel grows from the line, then settles beside it
- **Template/File:** `components/Contact.jsx`, `app/globals.css`
- **Record:** `contact-grow-then-settle`
- **What changed:** Restored `.contact-panel` to `left: 34.5%` desktop / `22%` mobile (guide and marker back to `22%` on mobile) and added an `x` tween that starts the collapsed panel centred on the guide (`-offsetWidth / 2`) and settles it to `0`, deliberately lagging the scale tween. The form-open reset also sets `x: 0`.
- **Before:** With the panel centred on the guide its middle ended on the line; with a left-edge origin it grew with its left border welded to the line.
- **After:** The box expands centred on the line (square in its middle), then slides right so the finished panel sits beside the line rather than straddling it.
- **Why:** User asked for expansion to start from the line without the panel's middle ending on it.
- **Fix/Notes:** Key finding: tweening `x` and `scale` together with the same ease cancels exactly — visual left = `L - W/2(1-t) + W(1-t)/2 = L` — which pins the left border to the line and reproduces the earlier rejected behaviour. Measurement confirmed `leftDx: 0` at every fraction. Splitting them so `x` runs later (0.85 -> 1.0, `power2.inOut`) than the scale (0.75 -> 1.0) gives the intended two-phase motion. Verified after reload: `centreDx: 0` while the box grows through scale 0 -> 0.823 (centred on the line), then it slides to `leftDx: 0` / `centreDx: 342` at scale 1, final 684x432 inside the section. Form open 684x597 with `leftDx: 0`, four fields; closing restores 684x432 and the direct-email row. Mobile 430px: `leftDx: 0`, 322x260, no right overflow. Lint 0 errors / 13 warnings (baseline); isolated production build compiled; port 3100 untouched.

#### [FIX] Contact square now morphs into the card instead of cross-fading
- **Template/File:** `components/Contact.jsx`, `app/globals.css`
- **Record:** `contact-single-element-morph`
- **What changed:** Removed the separate `.contact-panel-marker` element (DOM, ref and CSS) and made `.contact-panel` itself the travelling square via non-uniform scale (`12 / offsetWidth`, `12 / offsetHeight`). The box holds `opacity: 1` for the whole timeline; only its children fade in, at 88%. The panel is centred on the guide with `xPercent: -50`, and the mobile guide moved to `50%` with the panel at `left: 50%`.
- **Before:** A 12px marker faded out while a separate full-size panel faded in, so the large box appeared to fade in rather than grow from the small one.
- **After:** One element travels the line as a real 12x12 square and grows continuously into the finished card, with the line running through its middle.
- **Why:** User supplied a reference showing the card centred on the line and reported the big box fading in instead of transforming.
- **Fix/Notes:** The fade was structural, not an easing problem: two elements swapping opacity can never read as a morph, so the marker was deleted and the panel given a collapsed state instead. Non-uniform `scaleX`/`scaleY` is what makes a 38%-wide, 432px-tall panel render as a true square; uniform scale would have produced a rectangle. Contents are held at `autoAlpha: 0` until 88% so they are never seen stretched, and the collapsed box is not clickable because `autoAlpha` applies `visibility: hidden` to the children. Verified after reload across 9 scroll fractions: box opacity stays 1.00 at every step, size goes 12x12 -> 430x273 -> 641x405 -> 684x432 with `centreDx: 0` throughout, and content opacity rises 0 -> 0.17 -> 0.58 -> 1 only at the end. Form open 684x597 centred and inside, four fields, direct-email hidden; closing restores 684x432 and the direct-email row. Mobile 430px: centred on the line, 404x260, no overflow either side. Mobile guide moved from 22% to 50% to match the centred composition - flagging as a deliberate visual change. Lint 0 errors / 13 warnings (baseline); isolated production build compiled; port 3100 untouched.

#### [FIX] Halve contact card height; re-anchor form across its min-height transition
- **Template/File:** `app/globals.css`, `components/Contact.jsx`
- **Record:** `contact-half-height`
- **What changed:** Reduced the contact card from 432px to 216px by halving its vertical contributors: panel padding `clamp(1.1rem, 1.9vw, 1.9rem)`, title `clamp(1.5rem, 2.9vw, 3.2rem)`, rule margins `clamp(1.35rem, 3vw, 2.6rem) 0 clamp(1.2rem, 2.6vw, 2.2rem)`, email `clamp(0.8rem, 1.25vw, 1.15rem)`, `min-height` 12.5rem -> 6.25rem (mobile 20rem -> 10rem, padding 2rem -> 1.1rem). The form-open re-anchor now runs every frame during the transition and again on `transitionend`.
- **Before:** Card was 684x432. Opening the form left it overflowing the section bottom.
- **After:** Card is 684x216 - exactly half - and the form settles flush with the section bottom.
- **Why:** User asked for the box height to be halved.
- **Fix/Notes:** `min-height` alone could not shrink the card because its height was driven by padding, the title, the rule margins and the email row; those were scaled down and the email row was kept to a single line (no wrap at 684px). Landed on 216px in two passes (192px first, then padding and rule spacing nudged up). Halving the card exposed a latent bug: `.contact-panel` transitions `min-height` over 0.5s, so the single-rAF re-anchor measured a mid-transition height and placed the form past the section bottom (`inside: false`, 528px tall). Replaced with a rAF loop plus a `transitionend` listener and a 900ms safety timeout, all cleaned up on unmount. Verified after reload: morph still runs 12x12 -> 539x172 -> 679x214 -> 684x216 at constant opacity 1 and `centreDx: 0`; form open 684x528 with `bottomGap: 0` and `inside: true`, four fields; closing restores 684x216 with the direct-email row. Mobile 430px: 404x130, centred, no overflow. Lint 0 errors / 13 warnings (baseline); isolated production build compiled; port 3100 untouched.

#### [FEAT] Contact card recoloured orange; fix card floating after form close
- **Template/File:** `app/globals.css`, `components/Contact.jsx`
- **Record:** `contact-orange`
- **What changed:** `.contact-panel` background `#293bea` -> `#c2410c` and `.contact-bracket` `#9ca7ff` -> `#ffd2b0`. Separately, the re-anchor effect now runs on close as well as open, skipping only the initial mount.
- **Before:** Card was blue. Closing the form left the shrunken card floating 312px above the section bottom.
- **After:** Card is orange and sits flush with the section bottom in every state.
- **Why:** User asked for an orange box.
- **Fix/Notes:** Colour was chosen against contrast rather than by eye. The design system's warm token is Sunrise `#ffbf73`, but white-on-Sunrise is 1.62:1 and unusable for the card's white text; the outgoing blue set the quality bar at 7.22:1. `#c2410c` gives 5.18:1 with white, clearing AA 4.5 even for the small mono email line, and the bracket tint `#ffd2b0` gives 3.72:1 on the new fill (AA large). No other contact styles needed touching - the form inputs, submit button and rules are all white/alpha and hue-agnostic. Verified the recolour did not disturb the morph. While screenshotting, spotted a regression from the previous change: the anchor effect early-returned unless `formOpen` was true, so closing the form left the old 528px offset with a 216px card (`bottomGap: 312`). Now both transitions re-anchor, guarded by a `skipFirstAnchor` ref so the collapsed pre-scroll state survives mount - confirmed the travel state is still 12x12. Verified: FINAL / FORM / CLOSED all report `bottomGap: 0`, `inside: true`, `centreDx: 0`, bg `rgb(194, 65, 12)`; mobile 430px 404x130 with no overflow. Lint 0 errors / 13 warnings (baseline); isolated production build compiled; port 3100 untouched.

#### [FIX] Contact card centres vertically in its section instead of bottom-anchoring
- **Template/File:** `components/Contact.jsx`
- **Record:** `contact-centre-y`
- **What changed:** `finalY()` and the form re-anchor now resolve to `(section.clientHeight - panel.offsetHeight) / 2` instead of `section.clientHeight - panel.offsetHeight`.
- **Before:** The card rested flush with the section's bottom edge, sitting at the very foot of the viewport and clipping on shorter viewports.
- **After:** The card sits in the vertical middle of the contact section in every state.
- **Why:** User reported the card landing way too low rather than in the middle of the section.
- **Fix/Notes:** The bottom-anchored target was a leftover from when the panel was 432px tall and had been carried through every subsequent change; `bottomGap: 0`, which earlier rounds treated as the success signal, was in fact the defect. Both the scroll timeline's landing point and the form-open/close re-anchor use the centred value, so the square still travels to exactly where the card materialises. Verified after reload: closed `topGap` 228 / `bottomGap` 228, form open 72 / 72, closed again 228 / 228, all `centredY: true`, `centreDx: 0`, `inside: true`. Morph unchanged: 12x12 held through 75% of the timeline then 641x203 -> 684x216 at constant opacity 1, in view at every fraction. Mobile 430px: 404x130 with `topGap` 182 / `bottomGap` 182 and no right overflow. Lint 0 errors / 13 warnings (baseline); isolated production build compiled; port 3100 untouched.

#### [FIX] Contact card reveals pre-rendered content via clip-path instead of scaling
- **Template/File:** `components/Contact.jsx`, `app/globals.css`
- **Record:** `contact-clip-reveal`
- **What changed:** Replaced the `scaleX`/`scaleY` morph and the content fade with an animated `clip-path`. The panel now sits at full size with `overflow: hidden`, starting at `inset((h-12)/2px (w-12)/2px)` and opening to `inset(0px 0px)`. `will-change` updated to `transform, clip-path`; the form re-anchor also resets the clip.
- **Before:** The panel was scaled from a 12px square, which scaled its text too, and the contents were faded in at 88% so they appeared during the scroll.
- **After:** The title and email are rendered at final size and full opacity from the start; a 12px window travels the line and opens to reveal them.
- **Why:** User supplied a reference showing full-size content clipped by the growing box, and asked for the content to already be there rather than appear on scroll.
- **Fix/Notes:** Scaling was the wrong mechanism for this effect - a transform scales children, so the text could never sit at final size mid-reveal, and hiding the distortion is exactly why the fade existed. Animating `clip-path` decouples the two: layout and type stay fixed while only the visible window changes, and it stays GPU-friendly (no width/height layout animation). GSAP interpolates the numbers inside the `inset()` string directly. Verified after reload: `titleSize` is a constant 492x49 and `titleOp` 1.00 at every scroll fraction while the window grows 12x12 -> 430x139 -> 641x203 -> full, panel opacity constant 1. Checked `overflow: hidden` does not clip any state - `scrollHeight === clientHeight` for final (216), form (528), closed (216), mobile (130) and mobile form (453). Card stays vertically centred (`topGap === bottomGap`) in all states; four form fields present. Lint 0 errors / 13 warnings (baseline); isolated production build compiled; port 3100 untouched.

#### [CONFIG] Add .npmrc so Vercel can install dependencies
- **Template/File:** `.npmrc`
- **Record:** `vercel-eresolve`
- **What changed:** Added `.npmrc` with `legacy-peer-deps=true`. `package.json` and `package-lock.json` are unchanged.
- **Before:** Vercel failed at `npm install` with ERESOLVE - `@react-three/fiber@9.7.0` peers `react >=19 <19.3`, the project is on `react ^18.2.0`.
- **After:** `npm install` with no flags succeeds; `next build` completes and the built app renders.
- **Why:** Deploy `f449e10` failed before the build step.
- **Fix/Notes:** Two wrong turns, both caught by testing rather than reasoning. (1) Removing `@react-three/fiber` and `three` as unused - they are imported by `@shadergradient/react`'s dist even though that package declares no dependencies, so the build failed with `Can't resolve '@react-three/fiber'`. (2) Downgrading fiber to `^8.18.0` for React 18 - it installed and built, but the app died at runtime with `Cannot read properties of undefined (reading 'ReactCurrentBatchConfig')` and rendered zero sections, because Next 16 vendors its own React 19 for client bundles regardless of the installed React 18, and fiber 8's reconciler expects React 18 internals. Fiber 9 is therefore the correct version for this stack and the existing tree was right; only npm's resolution check needed addressing. Regenerating the lockfile was also rejected - it drifted `react-icons` 5.3.0 -> 5.7.0, where Simple Icons had dropped `SiMicrosoftazure`, breaking `data/skillsData.jsx`. Verified by simulating Vercel in a clean copy: `npm install` with no flags (699 packages, no ERESOLVE), `next build` exit 0, then `next start` on port 3312 - 6 sections, 7 planets, sun GIF present, contact card 684x216 `rgb(194, 65, 12)` centred at 228/228. The hero `<canvas>` does not mount, but this is pre-existing and identical on the dev server (3100 also reports `canvas: false`); `HeroGradient` documents a static CSS gradient fallback for that case. Port 3100 was never interrupted. Follow-up worth considering: React 18 -> 19 to match what Next 16 actually runs, which would remove the need for legacy-peer-deps.

#### [FIX] SEO: make every indexable URL reachable from the homepage
- **Template/File:** `app/page.jsx`, `app/blog/[slug]/page.jsx`, `scripts/gsc-audit.mjs`
- **Record:** `gsc-zero-indexed`
- **What changed:** The homepage's existing crawlable `sr-only` nav now links to `/work`, `/services`, `/contact` and every blog post (fetched from the DB with a try/catch fallback, page switched to `revalidate = 3600`). Homepage H1 and ProfilePage JSON-LD name aligned with the page title. Blog post pages moved from `force-dynamic` to `revalidate = 600`. Added `scripts/gsc-audit.mjs`, a zero-dependency Search Console URL Inspection auditor.
- **Before:** 0 of 27 URLs indexed. 23 reported "URL is unknown to Google" (never crawled), 4 "Crawled - currently not indexed".
- **After:** All 27 sitemap URLs are one hop from the only page Google actually crawls; blog URLs are cacheable.
- **Why:** User reported SEO broken across Search Console and asked for an API-based diagnosis.
- **Fix/Notes:** Diagnosed with the Search Console API rather than by guessing. Live checks had already cleared the usual suspects (robots.txt allows, 200 to Googlebot on all 27, no `noindex`, no `x-robots-tag`, self-referencing canonicals, valid sitemap, SSR content with JSON-LD), and the API confirmed it from Google's side: 0 canonical mismatches, `robotsTxtState: ALLOWED`, `indexingState: INDEXING_ALLOWED`, `pageFetchState: SUCCESSFUL` on everything crawled. The real finding was the link graph: 23 of 27 URLs had never been crawled and were only ever advertised via the sitemap, and `/blog` - the page linking to all 19 posts - was itself never crawled, so no post was discoverable by following links. Only `/` and 3 posts had ever been fetched. `sitemap.txt` shows 27 submitted / 0 indexed; `sitemap.xml` has been pending since 2026-08-04 and never downloaded, though it serves correctly (200, `application/xml`, 114ms), so that is Google deprioritising rather than a fault. Secondary finding: blog routes were `force-dynamic`, so every crawl was an uncached DB round trip (~1.6-1.8s TTFB, `x-vercel-cache: MISS`) against ~0.13s cache HITs for static pages. Verified: build compiles, homepage stays prerendered (`○ / 1h`), and the built HTML emits all 26 other URLs as real anchors with no JS - a scripted diff against the sitemap reports 27/27 reachable, 0 missing. Lint 0 errors / 13 warnings (baseline). Port 3100 untouched. Note: `revalidate = 600` means post edits take up to 10 minutes to appear publicly; revert that line if immediate publishing matters more. Still open, and not code-fixable: `/contact` (46 words) and `/services` (88 words) are thin, and the root domain `maoverse.xyz` is a parked GoDaddy page, so this subdomain carries no domain authority.

#### [FIX] Blog posts were still uncached; move admin check out of render
- **Template/File:** `app/blog/[slug]/page.jsx`, `app/api/auth/me/route.js`, `components/blog/AdminEditButton.jsx`
- **Record:** `blog-isr-not-applied`
- **What changed:** Added `generateStaticParams` to the post route and moved the admin check out of the server render into a client leaf backed by a new `/api/auth/me` endpoint.
- **Before:** Despite `revalidate = 600`, the route built as `ƒ` and Vercel returned `x-vercel-cache: MISS` on every request at ~1.0-2.7s TTFB.
- **After:** Route builds as `●` with all 19 posts prerendered at 10m revalidate; locally they serve in ~4ms.
- **Why:** Post-deploy check showed the previous caching fix had not actually taken effect in production.
- **Fix/Notes:** Two independent reasons the earlier `revalidate` was inert, both found by re-measuring rather than trusting the change. First, `verifyAuth()` was called during render solely to decide whether to show an "Edit Post" button; it reads `cookies()`, which opts the entire route out of static rendering no matter what `revalidate` says. That check now lives in `AdminEditButton`, a client leaf that queries `/api/auth/me` after mount and renders nothing for anonymous visitors - the same output the server produced for them - with the endpoint marked `private, no-store`. Second, a dynamic segment with no `generateStaticParams` has no slugs at build time, so Next renders it on demand and `revalidate` never applies; it now enumerates slugs from the DB, wrapped in try/catch, with `dynamicParams` left at its default so posts added later still render on demand and then cache. Verified: build shows `● /blog/[slug] 10m` with all 19 posts listed, pages return 200 at ~0.004s TTFB, article text intact (629 words SSR), canonical correct, "Edit Post" absent from anonymous HTML, `/api/auth/me` returns `{"isAdmin":false}` anonymously, and Playwright reports 0 console errors with the button hidden after hydration. Not verified: the button's appearance for a signed-in admin, which needs real credentials. Lint 0 errors / 13 warnings (baseline). Port 3100 untouched.

#### [FIX] SEO: site-wide visible footer navigation
- **Template/File:** `components/SiteFooter.jsx`, `app/layout.tsx`, `app/globals.css`
- **Record:** `gsc-discovered-not-crawled`
- **What changed:** `SiteFooter` gained a four-link nav (Projects, Services, Blog, Contact) and is mounted in the root layout, so every page carries visible internal links. Added `.site-footer-nav` styles with a 24px minimum target size and a visible focus ring.
- **Before:** Links to `/work`, `/services` and `/contact` existed only inside the homepage's visually hidden `sr-only` block; every other page was a link dead end. The footer component existed but was imported nowhere.
- **After:** All six checked routes serve 4 distinct visible nav links in the initial HTML.
- **Why:** Search Console still reports 23 URLs "Discovered - currently not indexed" with `Last crawled: N/A`.
- **Fix/Notes:** Re-checked the live site before changing anything. The earlier ISR fix did land - blog posts now return `x-vercel-cache: HIT` at ~0.17s versus `MISS` at ~1.6s before - and the homepage link block is live. The remaining weakness was that those 26 links sat in an `sr-only` container: Google follows hidden links, which is why the URLs moved from "unknown" to "Discovered", but they carry far less weight than visible ones, and no page other than the homepage linked anywhere. Note the status shift is itself progress: 23 URLs were "URL is unknown to Google" and are now "Discovered", so the sitemap and linking work is being seen; they are queued, not blocked. Verified: build compiles, all six routes emit the footer with 4 distinct links in HTML with no JS, desktop footer 62px on ink `rgb(16, 24, 40)`, mobile 390px has no horizontal overflow and all links visible. First mobile pass measured an 18px tap target, below the 24px minimum, so link padding was added and re-measured at exactly 24px. Playwright reports 0 console errors. Lint 0 errors / 13 warnings (baseline). Port 3100 untouched. This is reversible in one line by removing `<SiteFooter />` from the layout if the footer is unwanted.

#### [REVERT] Remove site-wide footer
- **Template/File:** `app/layout.tsx`
- **Record:** `footer-revert`
- **What changed:** Unmounted `<SiteFooter />` from the root layout and removed its import. The component and its `.site-footer-nav` styles are left in place, unimported.
- **Before:** Footer nav rendered on every page.
- **After:** No footer, matching the layout before this session.
- **Why:** User asked for it back out after I confirmed it was not necessary for the indexing problem.
- **Fix/Notes:** I had framed the footer as an SEO fix; on review it was marginal. The URLs are already "Discovered", so discovery was never the constraint - crawl demand is, and that is driven by external authority rather than internal linking. I also corrected an earlier inaccuracy: I had claimed no page except the homepage linked anywhere, but blog posts already carried 5 internal links each (home, /blog, and 3 related posts). The footer only meaningfully changed /work, /services and /contact, from about 1 link to 4, and those pages are not being crawled anyway. Component and CSS are preserved rather than deleted so it can be re-enabled with a single import if wanted later. Verified after revert: build compiles, no `site-footer-nav` in any served page, and the homepage still emits all 19 post links plus /work, /services and /contact. Also confirmed while here that `/login` returns `noindex, nofollow`, so it is not consuming crawl budget. Lint 0 errors / 13 warnings (baseline). Port 3100 untouched.

#### [FIX] Sitemap cannot silently shrink when the DB is unreachable
- **Template/File:** `app/sitemap.ts`, `data/blogSlugsFallback.json`, `scripts/refresh-sitemap-fallback.mjs`
- **Record:** `sitemap-truncation`
- **What changed:** The DB query retries once and, if it still fails, falls back to a committed snapshot of the 19 published slugs instead of an empty array. Added the snapshot and a script to regenerate it.
- **Before:** `catch {}` swallowed DB errors and `seededBlogPosts` is now empty, so a DB blip produced a sitemap with only the 8 static routes.
- **After:** 27 URLs are served whether the DB is up or down, and the build stays green either way.
- **Why:** Investigating why `sitemap.xml` has been stuck pending since 2026-08-04.
- **Fix/Notes:** Verified the XML itself is not malformed - well-formed, correct namespace, 27 URLs, 19 valid W3C `lastmod` values, no BOM, no future dates, 200 as `application/xml` in ~114ms - so "always fails" is a stuck pending registration rather than a defect. The likely trigger is this truncation bug: a failed DB read at fetch time would have served a sitemap missing every post. First attempt rethrew on failure, on the reasoning that an error is safer than a shrunken sitemap because Google retains the last good copy. Testing disproved that as a whole solution: with the DB unreachable the build aborted on `/sitemap.txt` prerender, so a DB outage would block deploys entirely. Replaced with a committed slug snapshot, which keeps the sitemap complete without coupling deploys to DB availability. Verified both paths end to end: healthy DB gives HTTP 200 with 27 `<loc>` and 19 `<lastmod>`; with `TURSO_DATABASE_URL` pointed at a nonexistent host the build still compiles and the route still serves 27 `<loc>`. Lint 0 errors / 13 warnings (baseline). Not done: deleting and re-submitting `sitemap.xml` in Search Console - the supplied OAuth token carries only `webmasters.readonly`, so `DELETE` and `PUT` on the sitemaps endpoint both returned 403 `PERMISSION_DENIED`; that needs the `https://www.googleapis.com/auth/webmasters` scope.

#### [FEAT] Add AZ-104 certification with two-star rating
- **Template/File:** `data/siteContent.ts`
- **Record:** `certifications`
- **What changed:** Added AZ-104 — Azure Administrator, Microsoft, Associate, with `stars: 2`. GH-300 already had `stars: 2`, so no duplicate change was needed.
- **Before:** Certifications list contained AB-100, AI-102, GH-300 (already 2 stars), and AZ-900.
- **After:** Certifications ×5 displays AZ-104 with a two-star rating; GH-300 remains two stars.
- **Why:** User requested AZ-104 2 stars and GH-300 2 stars.
- **Fix/Notes:** Confirmed the existing `CertificationItem.stars` field is rendered as filled stars out of 3 by `components/Achievements.jsx`; added the new item to the existing typed array without changing the component. Lint 0 errors / 13 warnings (baseline); isolated clean-install production build compiled. Port 3100 untouched.

#### [FIX] Certifications card no longer clipped after adding AZ-104
- **Template/File:** `components/Achievements.jsx`, `app/globals.css`
- **Record:** `certifications`
- **What changed:** Widened the certifications card to 252px, tightened row spacing to `gap-2.5`, rebalanced all four floating offsets so the composition is centred on the stage (+/-350px instead of -280/+420), and added `.achievements-float` scale-downs at short viewport heights (0.92 under 900px, 0.82 under 800px).
- **Before:** With a fifth credential the card ran 81px past the section floor and `overflow: clip` cut AZ-900 in half. At 820px viewport an achievement card was clipped too.
- **After:** No clipping at 720px, 820px, 900px or 1000px viewport heights.
- **Why:** User reported the achievements area looked wrong and asked to either merge it with the journey section or lengthen it.
- **Fix/Notes:** Measured before changing anything: the card was 437px tall, sitting at 644px in a 1000px section, overflowing by exactly 81px. Merging the sections was not the right lever - the ancestor chain showed Achievements inside `work-stage-pin` within a 4200px `pin-spacer`, so the apparent 3200px gap is pinned scroll distance rather than empty layout. Growing the section was also wrong because the stage is pinned at viewport height. The real defect was that the floating offsets are absolute pixels while the stage tracks viewport height, and the composition hung 70px below centre, so it clipped first on short viewports. Verified across four heights: 1000px (cert bottom 910, 90px clear), 900px (823, no clip), 820px (70px clear), 720px (0.82 scale applied, no vertical clip, no horizontal overflow). Confirmed visually that all five rows including AZ-900 render in full. Lint 0 errors / 13 warnings (baseline); isolated clean-install build compiled. Note: this session's working tree also contained stale copies of `app/page.jsx` and `components/work/WorkStage.jsx` that predated commits d95c1cf..34c4666 and would have reverted the continuous journey flow; both were restored to HEAD and the pre-restore copies saved to `/tmp/portfolio-worktree-backup/`. The fix above was then re-verified against the current continuous layout, with Timeline and Achievements both inside `#journey`.
