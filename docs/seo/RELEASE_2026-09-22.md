# SEO and security follow-up release

This follows the initial [audit](AUDIT_2026-09-22.md). Earlier findings and test
counts are historical; this document records the follow-up authorized by “do as
much as u can do and push”. Target: existing `main` upstream, `v2/main` at
`Yadui/portfolio-v2`, not the older `origin` repository.

## Completed

- Corrected AVD overview and both KV source files, removing unsupported deployment
  stories and numeric claims. Source-only commit `560ba1b` is pushed; Vercel marked
  that commit successful. See [correction details](ARTICLE_CORRECTIONS_2026-09-22.md).
- Blog edits preserve stored slugs, ignore caller slug overrides, validate IDs and
  field types, return 404 for missing posts and parameterize updates. Revalidation
  uses the stored article path plus index, RSS and sitemaps. This affects the local
  deployment receiving the write; it does not purge a remote deployment's cache.
- AVD metadata now uses `Azure Virtual Desktop: Passwordless MFA with Entra ID`
  without changing the slug, article heading or schema headline. Other article
  titles retain their full meaning instead of mechanical 60-character truncation.
- Contact mail validates field types/lengths/email, escapes HTML and strips header
  controls. Fixed recipient unchanged; URL/file access disabled; STARTTLS required.
  Missing mail configuration returns 503 rather than false success. Provider errors
  return generic 500 without exposing message content or credentials. Rate limiting
  was not changed. No actual email was sent during verification.
- Updated Next/eslint-config-next to 16.3.5, Nodemailer to 10.0.10, Drizzle ORM to
  0.45.3, SimpleWebAuthn server to 13.3.3, Drizzle Kit to 0.31.11, PostCSS and
  baseline-browser-mapping, plus affected compatible transitive dependencies.
  React 18, Tailwind 3 and the existing application architecture remain.

Nodemailer 10 requires Node 20+, within Next's Node >=20.9 requirement. Its official
[changelog](https://github.com/nodemailer/nodemailer/blob/v10.0.10/CHANGELOG.md)
was reviewed; this app uses SMTP, not the changed SES integration. Real local
stream/JSON transport tests exercise the new installed mailer without network mail.

## Verification

- `npm test`: **112 passed**, including real Drizzle/in-memory SQLite edit tests,
  actual article metadata/rendering, contact boundary tests and local mail serialization.
- `npm run lint`: **0 errors / 12 existing warnings**.
- `npm run build`: **passed with Next 16.3.5**, isolated temporary file-backed
  libSQL fixture, admin disabled and fresh ephemeral signing key. No shared DB
  writes or environment-file edits. Existing middleware/Edge deprecation warnings
  remain. Do not deploy local `.next` fixture artifacts; deployment must rebuild.
- `npm audit --omit=dev`: **zero vulnerabilities** at verification time.
- Full audit: **zero high/critical; four moderate affected-package entries** in the
  development-only Drizzle Kit → esm-loader → core-utils → old esbuild chain.
  Root cause: development-server cross-origin access advisory GHSA-67mh-4wv8-2f99.
  The suggested forced fix downgrades Drizzle Kit to 0.18.1. No forced downgrade or
  incompatible override was applied. Do not expose Drizzle Studio/esbuild servers;
  reassess on the next tooling release or by 2026-10-22. Production audit excludes
  this development-only dependency chain, not a suppressed finding.
- Ego Lite on localhost: at 1440×900 and 390×900, normal/reduced motion both had
  matching viewport/document widths, one H1, inline code computed as `inline`.
- Local contact returned **503** for valid input with mail deliberately disabled,
  **400** for invalid input. No SMTP request. Robots/RSS/sitemap/llms returned 200;
  `/login` returned 404 in deployment-like admin-disabled mode.

## Still requires owner action or separate publication

1. **Rotate the previously exposed Turso token** and update every consumer before
   revocation. This pass removed it from source, not provider access or Git history.
   No provider rotation or validity probe was performed. Use the provider console
   and deployment secret store; never paste token values into chat or this repo.
2. **Publish corrected bodies and the three drafts only after editorial approval**,
   using a verified backup and existing slugs. Git pushes do not import Markdown
   into the shared database. Strip editorial metadata and duplicate H1 on import.
3. Recheck the actual deployment, email configuration/delivery and Search Console.
   No ranking guarantees, live SMTP test or authenticated provider change is claimed.
4. Follow-up reliability work: DB-outage responses, authentic modification dates,
   timestamp-default migration, GFM tables, parser-backed TOC, shared upload assets.
   No database migration was executed here.

## Rollback

The release has no schema migration or shared-data write. If application regressions
appear, use Vercel's previous deployment while diagnosing, or revert the relevant
commit in a new commit. Do not revert credential removal or deploy known vulnerable
dependencies casually; prefer a targeted corrective patch. Source-only article
corrections are independent of runtime code and need not be rolled back together.
