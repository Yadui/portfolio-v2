// Metadata and JSON-LD for /work/[company] are generated in the page itself
// (app/work/[company]/page.jsx) via generateMetadata + generateStaticParams,
// which gives Next.js one canonical source for each route's SEO data.
// This layout is intentionally a passthrough only.
export default function WorkCompanyLayout({ children }) {
  return children;
}
